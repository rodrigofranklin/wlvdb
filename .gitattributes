# renv/activate.R is generated verbatim by renv and carries template spacing.
renv/activate.R -whitespace

# Runtime definitions must have one checkout representation on every CI host.
*.R text eol=lf

# The Issue #13 evidence harness is authenticated byte-for-byte.
run_logs/issue13-native-gate-orchestrator-v5/*.ps1 text eol=lf
run_logs/issue13-native-gate-orchestrator-v5/README.md text eol=lf
docs/validation/issue-13.md text eol=lf

# This historical scientific input is verified against its exact Git blob.
complementar/wiodr16/china_hours_per_worker.csv text eol=lf
