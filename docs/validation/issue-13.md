# Issue 13 — validação do corte principal

Estado em 2026-09-05: **paridade científica aprovada; gate de merge pendente**. A campanha dos dois métodos principais atingiu `science-validated` às `08:35:12Z`; desempenho controlado, preparação e falhas de publicação ainda não estão integralmente aprovados. Os registros anteriores em `run_logs/` e nas campanhas externas permanecem preservados; seus resultados não são convertidos automaticamente em evidência da campanha atual.

## Escopo e referências

- Métodos executáveis: `wiodr13` e `wiodr16`. Dez métodos experimentais permanecem declarados, com cálculo e recálculo desabilitados; seus contratos e testes unitários isolados foram preservados.
- Referência científica original: merge do #12, `cc2c861`. Baseline executável de compatibilidade: `e2f4d6dae9a6d35c966b305fabac52e489faa3e7`, discriminado do commit original.
- Candidato congelado para a campanha 054: `972d9f8fc7a887b3db485080264f2958cce13cdd`.
- Configuração local efetiva: `D:/Trabalho/Code/wlvdb-issue13-main-054/campaign-v2.json`; configuração anterior preservada em `campaign.json`.
- Tooling científico: cópia privada do runtime externo da campanha 053, vinculada por arquivo e hash. Comparação: derivada separada em `comparison-tooling-v1/`, sem alterar o tooling ou o estado científico; não representa adoção de resultados científicos anteriores.
- Matriz: 14 pares científicos (cálculo, recálculos 1/4/5, seleções 4/5 e `workers=2`), duas preparações e dez falhas. São 40 cenários nessa matriz, além dos seeds auxiliares, e 41 comparações previstas.
- A ferramenta de papers foi removida no commit `972d9f8`, junto com sua execução e dependências. `papern` e `prepaper` permanecem apenas nos valores padrão por compatibilidade de assinatura; qualquer tentativa de ativação falha no preflight. Não há cenário de papers neste gate.

As preparações usam os mesmos seis caches oficiais: WIOD13 (`WIOTS_in_MATLAB.zip`, `Socio_Economic_Accounts_July14.xlsx`), WIOD16 (`WIOTS_in_R.zip`, `Socio_Economic_Accounts.xlsx`) e EU KLEMS (`Statistical_Capital.rds`, `Statistical_National-Accounts.rds`). A verificação normativa de tamanhos e SHA-256 será executada antes da preparação; a igualdade real ainda não foi atestada por esta campanha.

## Evidência registrada

| Verificação | Resultado conhecido |
| --- | --- |
| Catálogo, configuração, perfis de saída e agregação | Suítes focadas passaram durante o fechamento do corte. |
| Execução nativa, planejador e consistência dos slices terminais | Suítes focadas passaram com fixtures; experimentais habilitados apenas em catálogos de teste em memória. |
| Catálogo publicado em `docs/methods.md` | Regeneração e `--check` passaram. |
| Suíte completa de fixtures | 66 arquivos passaram em quatro shards antes do commit `972d9f8`; registros `unit-tests/shard1.json` a `shard4.json`, com 242,865 / 387,4972 / 97,33 / 249,79 segundos. |
| CI | Commit `21cf753f6d51b7219539624cd0ca3f8e78028825` aprovado no run `33968224908`: Ubuntu terminou às `13:21:27Z` e Windows às `13:28:42Z`, ambos sem repetição. O commit anterior `a7c19fc` também passou no run `33966570786`. |
| Controlador suplementar | Parser passou; smoke de processo verificou captura de logs, erro de processo e escrita atômica de estado. |
| Ambiente dos filhos | Teste confirmou 35 variáveis removidas e dez definidas; biblioteca R preservada. |
| Isolamento e retomada do suplemento | Casos de igualdade/ancestralidade de paths e identidade PID + horário de início passaram. |
| Inicialização suplementar | Plano de 12 registros construído e auditor independente passou. Vínculo do controlador atualizado após sua estabilização, preservando a inicialização provisória. Nenhum cenário de preparação/falha foi executado. |
| Execução científica real | Os 28 cenários passaram; a campanha terminou às `2026-09-05T04:39:53.906680Z`, estado `science-completed`, revisão 103. Todos os registros de processos confirmam cluster encerrado. Isso ainda não constitui paridade integral. |
| Comparação antecipada WIOD13 | Segunda tentativa passou às `2026-09-05T02:31:28Z`: 21 artefatos, zero diferenças de indicadores e nenhum artefato ausente/excedente. Oito diferenças arquiteturais foram validadas pelas regras explícitas existentes. Sem repetição de cálculo. |
| Comparação antecipada WIOD16 | Passou às `2026-09-05T02:59:38Z`: 21 artefatos, zero diferenças de indicadores e nenhum artefato ausente/excedente. Oito diferenças arquiteturais foram validadas pelas mesmas regras explícitas. Sem repetição de cálculo. |
| Desempenho controlado | Onze de 14 pares aprovados em tempo/RSS às `13:26:44Z`, zero reprovados: os seis pares WIOD13 com `workers=1`, WIOD16/cálculo com `workers=1/2` e WIOD16/recálculos completos nos estágios 1/4/5. Todos passam também no limite relativo de 120%. Os quatro certificados de seeds passaram. Restam quatro medições nativas: candidato seletivo WIOD16/estágio 4, os dois seletivos WIOD16/estágio 5 e baseline WIOD13/`workers=2`. O mesmo controlador PID `51444` continua ativo. |
| Comparações finais | As 38 execuções terminaram às `08:10:37Z`: 14 paridades, quatro equivalências estritas `workers=2` versus `workers=1` e 20 verificações internas do oráculo. Os dez deltas foram aprovados às `08:35:12Z`: nove `exact-to-full` e um `baseline-known-divergence`, reproduzindo a divergência histórica WIOD13/estágio 4. Estado `science-validated`, revisão 230. |
| Preparação e dez falhas | Pendentes; serão executadas sem sobreposição com as medições controladas. O PR permanece draft e o canal público não foi alterado. |

A inicialização suplementar provisória terminou em `2026-09-05T01:01:48Z` (2026-09-04 no horário local). Seu plano tem SHA-256 `7fa43b85892dc2b34ccd9113c21d0edfa31a7d0241bb127037101210093732c3`; o auditor, `f08bdc7cafb39136fb1de0de6a77fddffe54052fed0f9376f970f80f7a55a950`.

Após estabilização do controlador compartilhado, a vinculação foi atualizada em `2026-09-05T01:05:37Z`, reaproveitando o mesmo plano/auditor, sem repetir o builder nem executar cenários. Os arquivos anteriores de estado e vínculo foram preservados, com hashes conferidos, em `D:/Trabalho/Code/wlvdb-issue13-main-054/control/supplemental/initializations/before-controller-finalization/`. O novo `tooling-binding.json` tem SHA-256 `9547fc55f2f322b1db655053fac3969b71ed2accc4e82f06ffbd6ee942c7cc16`. O estado confirmado é `initialized`, com zero cenários e zero comparações concluídos no suplemento.

## Ações operacionais

As duas primeiras tentativas de setup da 054 não produziram evidência científica: a primeira falhou na captura de `LASTEXITCODE` após inspeção Git; a segunda detectou que o PowerShell vinculado havia sido atualizado pelo aplicativo antes de iniciar R. Os diagnósticos foram preservados. A revisão operacional `054v2` mantém os mesmos roots de dados e separa controle/evidência em `control-v2` e `evidence-v2`; foi vinculada a uma cópia privada do PowerShell para não depender de futuras atualizações do aplicativo. Esses eventos não são resultados de paridade nem novos RunAll científicos.

Na revisão v2, a tentativa 1 parou no bootstrap em cerca de quatro segundos, antes do cálculo: o layout do checkpoint não correspondia ao contrato histórico do harness. A correção ficou restrita ao frontend das novas tentativas: `specs` e `evidence` passaram a ser diretórios irmãos sob a mesma base, mantendo `job` e `outcome` no controle. A relação exigida entre o diretório de especificações e o terceiro ancestral da evidência do cenário foi conferida. Não houve alteração científica, recriação da campanha ou substituição dos roots; a tentativa anterior e seus diagnósticos foram preservados.

A tentativa 2 começou às `2026-09-05T01:23:32Z` (22h23min32 BRT de 2026-09-04), com controlador PID `58912`. O estado das `01:23:34Z` registra os quatro cálculos simultâneos como `running`: WIOD13 e WIOD16, baseline e candidato, todos com `workers=1`. Os logs baseline já alcançaram transformação de dados e Leontief, distinguindo esta execução real dos erros anteriores de bootstrap. O resultado científico ainda precisa terminar e ser comparado.

O canal público permanece intacto: a campanha opera nos seus canais e roots isolados, sem promover os resultados em andamento. A automação existente de acompanhamento foi reativada com o prompt atualizado para o escopo dos dois métodos principais; ela não amplia o escopo do gate nem constitui autorização de merge ou publicação.

O controlador científico PID `58912` encerrou após os 28 cenários, sem lock remanescente. O resumo compacto `evidence-v2/science-execution-summary.json` reúne os quatro vínculos de fontes e, por cenário, caminhos/hashes das provas, commits, intervalos, tempo, RSS e encerramento dos processos; SHA-256 `18c4fc78636ce0f44b1ab699db7f3f9c110ad0690f5a26020881e8bb0f0d8391`. Sua classificação é explicitamente execução, não certificação de paridade. A transição para as comparações usou os mesmos roots/commits, sem recalcular. Comando, PID, início e hash do controlador de comparação estão em `comparison-v2-001.process.json`; logs em `comparison-v2-001.stdout.log` e `.stderr.log`.

O controlador de comparação PID `46776`, iniciado às `2026-09-05T04:44:20.8588714Z`, permanece ativo com identidade conferida. As seis paridades WIOD13 aprovadas cobrem cálculo `workers=1`, recálculos completos 1/4/5 e seletivos 4/5. Das dez verificações desse método contra os respectivos parent runs, duas tiveram igualdade estrita e oito registraram diferenças permitidas para avaliação posterior. Nesses oito casos, `status=passed` atesta execução válida, não aprovação das diferenças: a validação normativa dos dez deltas do oráculo continua em `0/10`. Não há certificação global nem equivalência `workers=2` concluída nesta consulta. O scheduler trabalha em lotes de até dois comparadores e aguarda o encerramento do lote antes de abrir o seguinte; seu código não foi alterado durante a execução.

A comparação final `parity/calculate/wiodr16/workers1` terminou às `2026-09-05T05:24:30.8382430Z`, com código zero e `comparison.passed=true`: 21/21 artefatos aprovados, quatro arrays sem divergências, nenhuma diferença de indicadores e nenhum artefato ausente/excedente inesperado. O contrato validou as oito diferenças arquiteturais declaradas e o recurso exclusivo do candidato. Prova: `evidence-v2/comparison-attempts/parity__calculate__wiodr16__workers1/attempt-0001/comparison.json`, SHA-256 `3e3d0a8c0985d1a281c006549a4006993d57fb97cc96049c2daa2606f32d546e`, conferido contra estado e outcome. O wrapper PID `64524` encerrou; na consulta das `05:35:41Z`, o recálculo WIOD16/estágio 1 do mesmo lote ainda estava ativo. Nenhum cenário científico foi repetido para obter essa comparação.

A paridade do recálculo completo WIOD16/estágio 1 terminou às `2026-09-05T05:41:17.0727432Z`, também com 21/21 artefatos aprovados e nenhuma divergência de arrays ou indicadores. Prova em `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage1__full/attempt-0001/comparison.json`, SHA-256 `671527c2d02657be2a59e716c6cf4b5392a0bd14e5cec7eda7044e6028194ad7`. Suas comparações contra os parent runs registraram somente `_anomalies.csv` no baseline e `_runtime_resources.rds` no candidato, respectivamente; a aceitação normativa dessas diferenças permanece pendente. Os hashes dos três reports foram conferidos contra o estado e os outcomes.

A paridade do recálculo completo WIOD16/estágio 4 terminou às `2026-09-05T06:22:29.7613497Z`, com código zero, `comparison.passed=true`, 21/21 artefatos aprovados e nenhuma diferença de indicadores ou artefato ausente/excedente inesperado. Prova em `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage4__full/attempt-0001/comparison.json`, SHA-256 `05ea982898d767aac1d68554a8d0517336b9cac72126945950ca553df5160d37`. O oráculo baseline do mesmo cenário terminou às `05:54:35.6889859Z`, com igualdade nos quatro arrays e diferença somente em `_anomalies.csv`; SHA-256 `64eedb488493d9bd4da9c3cc4dd1ad1fc0dbeed917b6586660af4e277c7bd73d`. Os hashes conferem com estado e outcomes. Esse resultado do oráculo não é aprovação normativa de seu delta; a avaliação conjunta permanece pendente. O lote seguinte iniciou às `06:22:30Z`, com o oráculo candidato do estágio 4 e a paridade completa do estágio 5.

O oráculo candidato WIOD16/estágio 4 terminou às `2026-09-05T06:27:36.7008295Z`, com diferença somente em `_runtime_resources.rds`, nenhuma diferença de indicadores e nenhum artefato ausente/excedente. SHA-256 de `evidence-v2/comparison-attempts/oracle__candidate__recalculate__wiodr16__stage4__full/attempt-0001/comparison.json`: `5df9409cb19d938c2448128a5e97539115d4e63e9b72ade5f2849c4ba5e02377`, conferido contra estado e outcome. Seu `comparison.passed=false` permanece como entrada para o agregado normativo, não aprovação automática da diferença.

A paridade do recálculo completo WIOD16/estágio 5 terminou às `2026-09-05T06:56:50.7237750Z`, com código zero, 21/21 artefatos aprovados, quatro arrays sem divergências e nenhuma diferença de indicadores. As oito diferenças arquiteturais declaradas e o recurso exclusivo do candidato foram validados pelo contrato. Prova em `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage5__full/attempt-0001/comparison.json`, SHA-256 `7e0146ba58d7bd90650146e95d4c62aa9ce2952a4a45d9d2952b5f41bab10dad`, conferido contra estado e outcome; o wrapper PID `30424` encerrou. As paridades de cálculo `workers=1` e recálculos completos 1/4/5 estão aprovadas para ambos os métodos. As seleções WIOD16, os casos `workers=2` e os deltas do oráculo ainda impedem a certificação integral.

As paridades seletivas WIOD16 também passaram: estágio 4 (`select-gross-output-mv`) às `07:21:40.9908960Z` e estágio 5 (`select-gross-output-du`) às `07:40:06.2462656Z`. Ambas aprovaram 21/21 artefatos, quatro arrays sem divergências e zero diferenças de indicadores. Os reports estão sob `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage4__select-gross-output-mv/attempt-0001/` e `parity__recalculate__wiodr16__stage5__select-gross-output-du/attempt-0001/`; hashes de `comparison.json`, respectivamente: `33605b5704f97b824795c176b73196b749a78a2d0596ecc8084e52849aeff4b2` e `df265f48962b6fc895658c4955479976709f511e12d3317b7d47a3ae5c728e98`.

Nos oráculos seletivos WIOD16, os dois baseline apresentaram igualdade estrita; os dois candidatos diferiram apenas em `_runtime_resources.rds`. Todos conservaram igualdade nos quatro arrays e nos indicadores. Os hashes dos três reports de cada seleção e dos respectivos outcomes foram conferidos contra o estado. Os deltas normativos continuam pendentes. Nenhum cálculo foi repetido para essas verificações.

As três comparações WIOD13 `workers=2` passaram e foram autenticadas contra estado, job e outcome. Paridade entre motores: `comparison.json` SHA-256 `890b75a69086ebb45d2e04d6885a51c6addd0b412f911d18ddcb459c12fd131e`, fim às `07:47:44.9565580Z`. Equivalência estrita baseline `workers=2` versus `workers=1`: `64146ac98b7c9a989f3a5259f2200a877e9352736110c81bd0bbca82f935eb64`, fim às `07:46:28.3221282Z`. Equivalência estrita candidato: `53589ef5462b8a6f5c118bd587c054ea5be57fe4d11547e0829071e3805020bf`, fim às `07:48:53.0480597Z`. Nas três, quatro arrays aprovados, zero divergências de valores/dimensões/dimnames/esquemas, zero transições fora da diagonal e zero diferenças de indicadores. As provas ficam sob `evidence-v2/comparison-attempts/`, nos diretórios dos respectivos IDs.

A CI `33951445936`, no commit de documentação `7ae28c1`, concluiu Ubuntu com sucesso às `07:13:25Z`. O job Windows `101266910993` falhou às `07:47:24Z`, durante o bootstrap; sua anotação oficial informa perda de comunicação do hosted runner com o servidor, e o log do job não estava disponível. Não é evidência de falha científica nem motivo para alterar o código. Foi solicitado `gh run rerun 33951445936 --repo rodrigofranklin/wlvdb --job 101266910993`; a API confirmou estado `queued`. Somente o job Windows foi reenfileirado; a campanha local não foi afetada.

A API confirmou depois a tentativa 2: novo job Windows `101273181707`, iniciado às `2026-09-05T07:52:48Z` e concluído com sucesso às `08:07:43Z`. O resultado Ubuntu anterior continua aprovado, com seu horário de início original; não foi reexecutado.

As três comparações WIOD16 `workers=2` também foram aprovadas, com os hashes de report e outcome conferidos contra o estado. Paridade: `47b0d2af6cae2bd23cff0e54319c21b8195bc0acec7272433171191fb6591601`, fim às `08:05:22.8092733Z`. Equivalência estrita baseline: `4467087fbfe8d7b4e70f3a300bce6625d5ca2d8e89623745a8f951783b5268ea`, fim às `08:10:36.9317483Z`. Equivalência estrita candidato: `2701e4f8718b2f0d9706be0c6cb3492b9d09ee08390e872e42c31236f7fcaf1c`, fim às `08:10:34.1437382Z`. Todas aprovaram quatro arrays, sem divergências de indicadores ou exceções de política. Isso não substitui a avaliação conjunta dos deltas de recálculo.

O primeiro `oracle-delta/recalculate/wiodr13/stage1/full`, tentativa 1, parou antes de criar `oracle-delta.json`. Seu job, SHA-256 `a644d203d4c37e42fdc467eecb851785e517e3800c1773576e5d3a69ce6ebed4`, e os quatro inputs permanecem autenticados. `worker.stderr.log` registra `Candidate architecture proof is invalid or misbound.`; o scheduler converteu o exit não zero no genérico `Oracle mismatch`, sem demonstrar uma divergência científica. A tentativa e seus snapshots foram preservados.

A causa é a exigência incondicional de `_nonfinite_resolution_diagnostics.csv` em dois guards do hardening histórico. Os perfis versionados de `wiodr13` e `wiodr16` são `nonfinite_none_v1`, ação `reject`, contagem esperada zero; o comparador autenticado exige esse diagnóstico somente se a contagem esperada for positiva. Os 14 reports de paridade contêm exatamente `_runtime_resources.rds` como recurso exclusivo do candidato, sem o diagnóstico de não finitos. A correção fica no wrapper do agregado, exigindo esse conjunto exato para os dois métodos, sem alterar harness congelado, comparações concluídas ou motor científico.

A revisão identificou ainda fingerprints distintos para deltas FST vazios: a construção original inclui os metadados dos dois endpoints mesmo com igualdade estrita e `mismatch_count=0`. O agregado valida o fingerprint de toda entrada, mas compara o delta vazio após confirmar booleanos estruturais, zero mismatches e ausência de diferença numérica/semântica; fingerprints de artefatos reprovados continuam obrigatórios e integrais. Não se introduz tolerância. O wrapper corrigido tem SHA-256 `0aba7bc37d5116f9028daf91c1d86e91bb980d0574917fcc7d1e365cf412b336`. Revisão independente e parse R passaram; não houve alteração do controlador ou do harness congelado.

O autoteste `oracle-delta-selftest-002/selftest.json` passou nos 69 checks em menos de um segundo, de `08:30:33.3899260Z` a `08:30:34.1431458Z`; SHA-256 `2f015f3700dd7af865d409eec81fb19606e4b4a822db9995df69ff011ae677d0`. Inclui os dez quartetos reais (nove deltas vazios e a divergência histórica WIOD13/estágio 4), identidade, inventário, fonte, perfil, fingerprints de falhas, zero exato, estados `NA`/`NaN`, indicadores e igualdade de todos os inputs antes/depois. Nenhum payload foi aberto ou cálculo executado. A tentativa `001` está preservada: os dez casos reais já passaram, mas um check de referência AST falhou porque o próprio visitor do teste removia slots `NULL`; somente esse visitor foi corrigido antes do `002`.

O controlador existente foi retomado às `2026-09-05T08:31:21.0372829Z`, PID `58076`, com `-MaxAttempts 2`; registro em `comparison-v2-002.process.json`. Seu SHA-256 permanece `99d61985db2290fdf6285b2d4a587a0d9943c1f2dcbaace688c028b2cfe0b416`. As 38 comparações concluídas são reautenticadas, não reexecutadas; somente os deltas pendentes recebem novas tentativas. A primeira tentativa falha e seu wrapper anterior continuam preservados.

Na tentativa 2, o primeiro delta retornou `passed=true`, `exact-to-full` e hashes baseline/candidato idênticos (`39b095edd487f99b122eb65b28cc2c359ea40719818d01dcd92aa120bfc9cf3c`). O controlador rejeitou seu envelope porque `Map()` nomeava os quatro inputs e o JSON os serializava como objeto, não array. Foi corrigida somente a serialização, com `unname(Map(...))`; o teste CLI também confirma os quatro vínculos no leitor PowerShell. A prova e o estado rejeitado da tentativa 2 foram preservados, sem adoção manual.

O autoteste final `oracle-delta-selftest-004/selftest.json` passou nos 70 checks, incluindo o CLI real e seu array de quatro inputs, de `08:34:19.0842257Z` a `08:34:20.2038516Z`; SHA-256 `db8249b3b0ed0199f7f5af0957e064ef724dd556b43478ea455a362b5659b002`. Wrapper final: `39d7249130e9715e7b2fbaa1b698b2292e45b01994c4a0936701231ddbd3223c`. O teste intermediário `003`, que detectou a nomeação automática do `Map`, também está preservado. A nova retomada do mesmo controlador, PID `32804`, começou às `08:34:57.3910107Z`, com `-MaxAttempts 3`; comando e hashes em `comparison-v2-003.process.json`. Nenhuma das 38 comparações nem dos 28 cenários foi repetida.

Essa retomada concluiu os dez deltas às `2026-09-05T08:35:12.2340432Z`, elevando o estado para `science-validated`, revisão 230. Nove são `exact-to-full`; o único `baseline-known-divergence` é WIOD13/estágio 4 completo, com projeções baseline/candidato idênticas para as diferenças SEA preexistentes. O controlador encerrou, sem stderr. Uma auditoria independente confirmou dez jobs, dez outputs, dez outcomes e 40 inputs por SHA-256, identidade, caminho e papel. As duas tentativas históricas falhas foram preservadas e autenticadas, sem convertê-las em sucesso.

O snapshot `evidence-v2/science-validated-state.json`, criado sem sobrescrita às `08:41:22Z`, conserva a evidência de 28 cenários, 38 comparações e dez deltas aprovados. Possui 161.308 bytes e SHA-256 `ddad11f8c4d80afd2c78f8c0d367a986e158a8e52a808c3fd02f0faab903acad`, idêntico ao estado original antes e depois da cópia. O motor científico permanece no candidato congelado `972d9f8`; os ajustes finais afetaram somente o agregado de validação e seu autoteste.

A comparação antecipada de WIOD13 (`early/parity/wiodr13/001`), executada de `01:44:49Z` a `01:47:23Z`, preservou comando, entradas e hashes em `early-parity-wiodr13-001/`. O erro `Candidate runtime generation differs from metadata derivation.` veio do perfil V5 derivado em `3ae99a848156a28431ff44cf4d9e619c6de84a83`, que espera geração `4668bf1eadb9ea3ea3f121a889f6b29016c1505f1dbcb3cc27ed3c02ab52c2dd`; o candidato congelado apresenta `600d8cdd2c692fea0b608285b84c3d260b123aac4cf1904ee8b6b997ec988c63`. O diagnóstico integral está em `execution-error.json`. Não houve `comparison.json`, escrita no estado científico ou reinício dos cálculos. O próximo ajuste deve reconstruir e verificar a equivalência dos metadados a partir dos commits congelados, em um vínculo de comparação separado; não basta substituir o hash esperado nem modificar o harness utilizado pelos processos científicos ativos.

Esse ajuste foi implementado por `tests/manual/issue13-main-build-metadata.R`: definições e configurações foram reconstruídas em três worktrees limpos, somente de código, nos commits `cc2c861`, `e2f4d6d` e `972d9f8`. Nenhum payload científico foi aberto ou calculado. Os seis sidecars do baseline original e do oráculo executável são exatamente iguais. As 12 tabelas por braço dos dois métodos também são iguais aos perfis históricos, sem diferenças de esquema, linhas, células ou ordem. As únicas três diferenças de envelope são commit candidato, geração e redução de 12 para dois métodos. Os negativos de método, geração, célula e ordem passaram.

Artefatos locais em `comparison-tooling-v1/`:

| Artefato | SHA-256 |
| --- | --- |
| `metadata-derived.json` | `8476d0389d7cdaf87c8d63e2be569ad00d4978627869810f560bc26fceecaf23` |
| `metadata-derivation-provenance.json` | `dde0c2e6b1c1c4dfaefa42341349c7bc67149f238ae1687fe30616fa6642dbdb` |
| `metadata-diff-vs-v5.json` | `edee1e23c59d43113ede4389677b38278a143435cd85bd7fc9a6bf369b74b161` |
| `comparison-binding.json` | `b464b4c29255feed2a30a41c59f22d9ef5df4dd9304efc01dcfd62e37871e3d9` |

O vínculo separado conserva 45 dos 47 arquivos byte a byte; muda somente o perfil de metadados e, deterministicamente, os guards de commit e métodos do validador. Também autentica os dois braços de execução, a proveniência semanticamente validada, snapshots do controlador e contratos de entrada por lado. Paridade aceita os commits diferentes dos dois motores; comparações internas usam o mesmo braço. Retomada com outro vínculo é rejeitada. Os 25 testes positivos/negativos passaram em `comparison-selftest-001/selftest.json`, sem alteração no tooling científico. A segunda comparação antecipada usa o worker `/2` e registra evidência em `early-parity-wiodr13-002/`; é diagnóstico antecipado, não adoção automática entre as 38 comparações finais.

A segunda comparação antecipada WIOD13 terminou com `passed=true` e `comparison_passed=true`, de `02:28:07Z` a `02:31:28Z` (cerca de 201 segundos). SHA-256 de `comparison/comparison.json`: `bf820579c98f74cc7803998dc2030445fdc7dab69e169a1186372118b233fcca`. O wrapper PID `50612` e o R PID `45888` encerraram. A comparação antecipada WIOD16, com o mesmo vínculo, em `early-parity-wiodr16-001/`, também passou: de `02:34:00Z` a `02:59:38Z`, cerca de 1.538 segundos; SHA-256 de `comparison/comparison.json`: `e6df7054cc0226149c38320616f73c887dea58144ac9dce3782d3b625c0273b4`. Seu wrapper PID `35104` e R PID `50552` encerraram. Ambas leem os cálculos já autenticados, não escrevem no estado principal e não repetem ciência. São provas antecipadas, ainda não adotadas automaticamente entre as 38 comparações finais. O commit de tooling `8543089` contém a correção; o candidato científico permanece em `972d9f8`.

### Reúso exato de provas de arrays

Os oito pares FST dos cálculos completos têm payloads byte-idênticos entre os motores. `tests/manual/issue13-main-array-proof-*` permite reaproveitar somente o retorno de `wlv13_compare_fst_array` já produzido nas duas comparações antecipadas autenticadas. A chave inclui par ordenado de motores/método, hashes e tamanhos dos payloads, papel lógico, sidecars semânticos completos com atributos, dimensões/dimnames, esquema FST, modo, chunking e hashes dos algoritmos/vínculos. Qualquer incompatibilidade segue pela comparação integral; nenhuma tolerância é acrescentada. Inventário, proveniência, validações antes/depois, demais artefatos e veredito continuam no comparador original.

O cache determinístico `array-proof-cache-v1/array-proof-cache.json`, 238.432 bytes, tem SHA-256 `2490e32895d34ea9029e2a24182bcbc609fb70d345329830508d35a6294d2326`, explicitamente permitido tanto pelo loader R quanto pelo validador PowerShell. Isso impede aceitar um cache fabricado apenas por ele declarar hashes e origens válidas. A reconstrução em `verification/cache.rebuilt.json` foi byte-idêntica. O selftest reproduziu exatamente os oito retornos reais e passou em 14 negativos, incluindo payload, nomes/atributos dos eixos, esquema, proof, contexto/motores, modo e chunking; os modos `strict` e `cross_engine_source_v1` com contexto nulo produzem miss. Log final: `array-proof-cache-v1/verification/selftest-final-14.stdout.log`, SHA-256 `2b2cd8356ea5f974d8fd512a5bdd4e0fff46db1df375b22ddad9ec264d8c0a61`. Não houve leitura dos payloads nesse selftest.

O canário de ponta a ponta `array-proof-canary-wiodr13-001/` passou com quatro hits e descompressão pelo comparador original proibida no teste. Os três CSVs foram byte-idênticos aos da comparação antecipada; o JSON foi semanticamente idêntico, excetuando somente `scenario_id` e `compared_at`. SHA-256 do novo `comparison/comparison.json`: `60682f894f464deed8d4c2c2cea309251cf239ec3121c48cb97fd7c507bdfbf9`. Seu intervalo conservador, de `03:34:50Z` até a confirmação de encerramento do PID em `03:39:24.8919588Z`, está em `comparison-interval.json` (SHA-256 `b414208ff1979c34ec08081f8afde4f1ac891a07feaf5580197aae09049e1cf9`) e é incluído no planejamento de desempenho. É diagnóstico sob carga concorrente, não medição normativa nem execução de cálculo.

O vínculo suplementar final é `array-proof-binding-v2/array-proof-binding.json`, SHA-256 `66d6fc7f75cfab3f946058fe9d0c6e49ff649d507791531e5afcdee867cd7504`. Ele possui snapshots próprios de entrypoint, biblioteca, builder, validador e cache; os 47 arquivos do vínculo original `b464...` permanecem inalterados. O snapshot provisório v1 foi preservado, não utilizado no controlador principal, após uma correção de miss para modos incompatíveis. A retomada verifica todos os attempts históricos antes do reparo, inclusive os já aprovados, e não permite habilitar, desabilitar ou trocar o vínculo silenciosamente. Passaram 45 verificações PowerShell em `array-proof-binding-selftest-004/selftest.json`, sem R/cálculo. A comparação integral dos quatro seeds de desempenho D↔C permanece no modo `strict`, sem este cache.

A inspeção dos demais perfis alcançáveis não exigiu mudanças: oito SHA-256 de `_unit_contract.csv`/`_source_manifest.csv` dos dados normalizados dos dois métodos/braços coincidiram com o perfil de preparação histórico. Os quatro `_anomalies.csv` de cálculo coincidiram com os fingerprints históricos dos bridges e parents de estágio 5; o target baseline WIOD13/estágio 1 também coincidiu. Os outros cinco targets de recálculo ainda precisam ser conferidos quando concluídos. Os perfis históricos de preparação, bridges e multiplicidade permaneceram byte-idênticos; seus artefatos e contratos atuais continuam sujeitos à comparação efetiva.

O suplemento da revisão v2 foi inicializado e auditado às `2026-09-05T01:18:52Z`, sem executar os cenários de preparação/falha: zero cenários e zero comparações concluídos. SHA-256 do plano: `e4ea4f7c1d575657d30699a9f721502ceeed7c34cb86ff5b76018322bf31dc72`; vínculo do tooling suplementar: `4cd8afad9dfc8b716713583ea500b8f126464c526c33561a9c59138041ac1fa2`. O controle anterior permanece preservado.

Às `2026-09-05T01:44:29Z`, somente o controlador suplementar foi reautenticado para registrar intervalos individuais de processos. Estado e vínculo anteriores foram preservados, com igualdade dos hashes confirmada, em `control-v2/supplemental/initializations/before-process-journal-20260905T014428967Z/`. O novo vínculo tem SHA-256 `73988e10a28537ce6f3e6fcec8ce6e1bb0f949c0f027f8746322b681563045fd`. Plano, auditor e inventário de logs permaneceram iguais: nenhum builder, auditor R, preparação ou cenário foi repetido. O journal começa vazio e conserva o limite histórico anterior em `01:18:52Z`, sem estendê-lo até a reautenticação. O estado científico não foi alterado.

Às `2026-09-05T03:03:19Z`, a validação anterior à importação do seed das falhas foi reforçada: resultado e métricas devem coincidir com os hashes registrados pelo estado científico, além de método, root, commit e encerramento do cluster. O seed real passou; alterações independentes dos dois hashes foram rejeitadas antes de R/importação. Somente o vínculo do controlador suplementar foi atualizado, para SHA-256 `602cfd1638a8a02359f0150b8e20af18f157aadd3d2240db9514190254e632ae`; a versão anterior está em `control-v2/supplemental/initializations/before-seed-provenance-20260905T030319669Z/`. Estado, plano e journal permaneceram byte-idênticos, com zero preparações/falhas executadas.

Executar a partir do checkout, usando o PowerShell selado definido na configuração. `Status` é somente leitura. As ações científicas e suplementares devem ser coordenadas pelo mesmo orçamento de recursos; não iniciar `Prepare` ou `Faults` como carga adicional não contabilizada enquanto o pool científico estiver cheio.

```powershell
$campaign = 'D:\Trabalho\Code\wlvdb-issue13-main-054\campaign-v2.json'
$runner = 'D:\Trabalho\Code\wlvdb-issue13-main-054\tools\powershell\pwsh.exe'
$comparisonBinding = 'D:\Trabalho\Code\wlvdb-issue13-main-054\comparison-tooling-v1\comparison-binding.json'
$arrayProofBinding = 'D:\Trabalho\Code\wlvdb-issue13-main-054\array-proof-binding-v2\array-proof-binding.json'
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Plan
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Initialize -ConfigPath $campaign
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action RunScience -ConfigPath $campaign -Arm all -MaxJobs 4
& $runner -NoProfile -File tests/manual/issue13-main-compare.ps1 -ConfigPath $campaign -ComparisonBindingPath $comparisonBinding -ArrayProofBindingPath $arrayProofBinding -MaxJobs 2
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Status -ConfigPath $campaign
```

O suplemento usa `tests/manual/issue13-main-supplemental.ps1 -ConfigPath $campaign -Action <ação>`:

| Ação | Efeito |
| --- | --- |
| `Initialize` | Constrói e audita o plano; não prepara dados nem calcula resultados. |
| `Prepare` | Confere os seis caches e executa as preparações baseline/candidata. |
| `Compare` | Compara semanticamente as preparações e os três seletores de artefatos. |
| `Faults` | Exige preparação comparada e cálculo candidato WIOD13 aprovado; importa o seed, prepara dez canais, executa dez falhas e valida o agregado transacional. |
| `RunAll` | Executa a sequência suplementar completa. |
| `Status` | Lê `control/supplemental/state.json`. |

### Desempenho controlado, separado da ciência

`tests/manual/issue13-main-performance.ps1 -ConfigPath $campaign -Action Plan` é somente leitura. Ele classifica os 14 pares, identifica quais braços podem ser aproveitados e estima o espaço adicional antes de qualquer repetição. `Status` recalcula a mesma classificação; `Run -MaximumRepeats N` executa no máximo N cálculos/recálculos necessários, sempre em série, após os 28 cenários científicos passarem. As comparações estritas dos seeds não consomem esse contador. `-ExecutionConfigPath` separa os roots/estado de desempenho dos roots científicos; `-ComparisonBindingPath` mantém o comparador autenticado da campanha original.

Uma medição científica só recebe `controlled-reused-singleton` quando seu intervalo UTC não se sobrepõe a nenhum outro job, comparador, preparação ou falha da campanha, incluindo tentativas malsucedidas e seeds auxiliares. O suplemento registra cada processo com início e fim reais em UTC, PID, ação e resultado, inclusive falhas; medições anteriores ou posteriores não são invalidadas retroativamente. Intervalos interrompidos sem encerramento comprovado ficam abertos. Somente o histórico anterior ao journal usa exclusão conservadora, com limite final fixado na migração; executar ações novas não estende esse limite sobre medições já concluídas.

WIOD13 baseline `workers=2` passou de `03:35:31.5580961Z` a `03:45:06.5480211Z`: 574,989925 segundos, pico RSS de 7.417.114.624 bytes e cluster encerrado. Essa medição **não** é reutilizável para desempenho, pois o canário de arrays se sobrepôs até `03:39:24.8919588Z`. WIOD16 baseline e os dois candidatos `workers=2` poderão fornecer até três medições reutilizáveis, somente após conclusão válida e confirmação de ausência de sobreposição. A admissão observada está limitando `workers=2` a um cenário de cada vez: duas reservas de 40 GiB mais piso de 16 GiB exigem 96 GiB livres; havia cerca de 68,17 GiB livres na consulta. O controlador congelado e as reservas não foram alterados para acelerar essa parte.

O `Plan` consultado às `04:23Z` confirmou duas medições `controlled-reused-singleton`: WIOD16 baseline, de `03:45:08.7291869Z` a `04:12:50.7079642Z`, 1.661,9787773 segundos e RSS de 21.301.354.496 bytes; WIOD13 candidato, de `04:12:52.8579963Z` a `04:19:59.9454732Z`, 427,0874769 segundos e RSS de 5.445.861.376 bytes. Ambos registraram dois workers, cluster encerrado e nenhuma sobra de processos. Restam 26 medições nativas e quatro comparações estritas dos seeds nesse plano parcial, ainda sem pares completos de desempenho. O candidato WIOD16 permanece pendente; WIOD13 baseline continua explicitamente excluído do reúso pela sobreposição com o canário.

Após a ciência terminar, o candidato WIOD16 `workers=2` também foi classificado como `controlled-reused-singleton`: `04:20:02.0302796Z` a `04:39:53.6647718Z`, 1.191,6344922 segundos e RSS de 16.656.515.072 bytes, dois workers, cluster encerrado e nenhum processo remanescente. SHA-256 do resultado: `e6139c9a0893772d30eeef3bab270255aa7f2c00fb4b2f7a06944a34a86a822d`; métricas: `a157a186a294f05a2ed82509443f1551af5c05274de336603b458b3bf2e58308`. O par WIOD16 `workers=2` passou: limite de tempo de 2.261,9787773 segundos e limite de RSS de 23.431.489.945 bytes. Há um de 14 pares completos, três medições reutilizáveis, 25 medições nativas restantes, quatro comparações estritas de seeds e zero seeds extras.

O snapshot `evidence-v2/performance-plan-science-completed.json`, SHA-256 `feb2046a2ce8e11e260ec0d01716c3e6d220883886f98ae3fe9dae4fecdabbba`, registra essa classificação. A estimativa adicional caiu para cerca de 57,77 GiB e cabe em C: preservando o piso de 10 GiB. Ainda não foram executadas repetições controladas no root C. Uma inspeção somente leitura também confirmou que a preparação usa o mesmo limite temporal com margem absoluta de 600 segundos e o mesmo critério de RSS; nenhum ajuste de fórmula ou de harness foi necessário.

Após `science-validated`, o plano foi atualizado em `evidence-v2/performance-plan-science-validated.json`, SHA-256 `51c816d1960333b03ec7b6b6db986217b2cada8540cbe61c34da9831b68932d2`: 25 medições nativas, quatro comparações estritas, zero seeds extras e um par já aprovado. A reserva adicional de 62.025.735.942 bytes cabe nos 94.836.264.960 bytes livres em C:, preservando o piso de 10 GiB. O controlador existente iniciou `Run -MaximumRepeats 25` às `2026-09-05T08:40:55.4854545Z`, PID `48948`, sem novo RunAll científico. Registro em `C:/Users/rodri/AppData/Local/wlvdb-issue13-performance-054/performance-c1-001.process.json`, SHA-256 `f34c0037c13b0906379056b32cedccfcca75b7f628a7b99ffa77b394018cad98`; stdout/stderr no mesmo diretório. A preparação e as falhas não executarão concorrentemente com essas medições.

O primeiro worker C1 parou de `08:41:26.1152117Z` a `08:41:27.0206067Z`, antes do cálculo, com `Scenario bundle binding differs`. O Windows/MSIX apresenta o diretório lógico `C:/Users/rodri/AppData/Local/wlvdb-issue13-performance-054` também pelo prefixo físico `C:/Users/rodri/AppData/Local/Packages/OpenAI.Codex_2p2nqsd0c76g0/LocalCache/Local/wlvdb-issue13-performance-054`; o R normalizou os paths para o segundo prefixo. A diferença exata que acionou a validação foi `scenario_evidence`, não método, commit ou channel. Não foram produzidos resultado científico nem métricas. A tentativa C1 inteira permanece preservada e não foi convertida em medição válida.

A correção operacional C2 está em `<prefixo-físico>/binding-c2/`: `campaign-performance-c2.json`, SHA-256 `503716e272a9025554ce441f4a22d4e3fbb2541753f6bbdb37da1d65413c4f65`, e `provisioning.json`, SHA-256 `615506215ede48d7adfdb0eaf0e800dc06d9ff1fffa516dbbbef72fa62939544`. Doze pares de aliases coincidiram por identificador NTFS e SHA-256, incluindo configurações, bindings, `.git` e manifestos. Os quatro worktrees e as fontes são os mesmos arquivos físicos; não houve cópia, movimentação ou exclusão. Somente os caminhos operacionais foram vinculados novamente, com controle/evidência C2 próprios. A derivação está registrada em `physical-rebinding.json` e no script local `run_logs/issue13-main-bind-physical-c2.ps1`; os inventários e fingerprints originais foram preservados.

O `Plan` C2, `performance-plan-initial.json`, SHA-256 `d4d925fd44a28679fc6e2747e351c24829cd0c4174329b69c853bf19c4914892`, manteve 25 medições, quatro comparações e a reserva de disco aprovada. A execução C2 iniciou às `08:46:29.3464470Z`, PID `45640`; registro `performance-c2-001.process.json`, SHA-256 `0b8d464f4d238872ca4c804c828a165f746bdda399a62c134111cf7d6740182a`. O worker PID `53168` iniciou às `08:47:02.0235166Z` e o R de cálculo PID `3680` às `08:47:03.878381Z`. A auditoria independente confirmou dez vínculos job/bundle/scenario-spec/process-spec, incluindo o path antes rejeitado. Nenhum controlador, worker, biblioteca ou motor foi alterado para essa correção. O acompanhamento existente foi atualizado e relido para usar C2 e não repetir os gates científicos concluídos.

O primeiro par novo de desempenho, WIOD13/cálculo/`workers=1`, passou nos dois limites; ambos os braços têm classe `controlled-serial-repeat`, sem sobreposição. Baseline: `08:47:03.8240755Z` a `09:01:28.7131183Z`, 864,8890428 segundos e RSS de 7.098.675.200 bytes. Candidato: `09:01:31.9797423Z` a `09:08:38.8255645Z`, 426,8458222 segundos e RSS de 5.256.273.920 bytes. Limites normativos: 1.464,8890428 segundos e 7.808.542.720 bytes. SHA-256 de `scenario-result.json` / `process-metrics.json`: baseline `8bfd8f391eedee13fb13fc54981b6b2dc13b7cd7efc8c6085d6733f157fd636c` / `aeabba424b19ef5f3c03bf48825bbce7302f1a24ce5d05f9eecf024fa0c4502f`; candidato `4fc3208efb7475237a313ea6b9e796bbcbbb3643f6a58f898d9e8f11388fcc57` / `b41a03ecfc0c77598a6c9a4db25a8e071191ddc9f97e4902e31ffe16e8720e66`. Os arquivos estão sob `binding-c2/evidence/performance/<braço>__calculate__wiodr13__workers1/attempt-0001/evidence/scenarios/<mesmo-id>/`.

O report atualizado às `2026-09-05T09:09:41.8702004Z` registra dois de 14 pares completos, zero pares reprovados e 23 medições nativas restantes. Uma auditoria independente conferiu os jobs, outcomes, resultados e métricas dos dois braços por hash e identidade, sem discrepâncias; ambos encerraram o cluster, sem processos remanescentes. O controlador iniciou `seed-parity/candidate/wiodr13` às `09:09:42.1963126Z`, PID `20844`; as quatro comparações estritas D/C ainda precisam terminar antes de usar os seeds nos recálculos. A CI do commit `c2e2b68` passou em Windows/Ubuntu no [run 33956386988](https://github.com/rodrigofranklin/wlvdb/actions/runs/33956386988). Não foram iniciados preparação, falhas ou testes R auxiliares durante esta checagem.

O par WIOD13/recálculo completo/estágio 1 também passou em tempo/RSS, com ambos os braços `controlled-serial-repeat`, sem sobreposição. A ordem alternada foi candidato (`09:11:14.3148320Z` a `09:15:28.1941958Z`, 253,8793638 segundos, RSS de 4.492.025.856 bytes), depois baseline (`09:15:58.9831189Z` a `09:23:48.5565810Z`, 469,5734621 segundos, RSS de 5.956.198.400 bytes). Limites: 1.069,5734621 segundos e 6.551.818.240 bytes. Os hashes de job, outcome, resultado e métricas foram conferidos; ambos tiveram exit zero, sem timeout, cluster encerrado e nenhum PID remanescente. SHA-256 de resultado/métricas: candidato `5d159fa8e44c54b9412c09f08a1eb8b6961475bca6c0a280de2dfc1bf57d4c67` / `a1a531c8f4dc177ff2f4d1c27c689125055c3210e8f5654b99304c38a38325ec`; baseline `58349b1eb5cddf9d5e1a9c5ff0c7e4ce6e92d65a4c91b5c94449e2a0e90f0b31` / `f6189add1a9e5349f378ba477df3573a7923822b9f10a54d4c000eabf0c5e29c`.

Às `09:23:49.9514163Z`, o report registra três de 14 pares aprovados, zero reprovados, 21 medições nativas e duas comparações de seeds ainda necessárias. O worker baseline WIOD13/estágio 4 completo iniciou às `09:23:50.2729809Z`, PID `22024`; a consulta das `09:27Z` confirmou o mesmo controlador ativo, sem stderr. As comparações de seeds WIOD13 já foram concluídas antes dos recálculos; preparação e falhas permanecem aguardando o término do desempenho.

Os certificados `seed-parity/baseline/wiodr13` e `seed-parity/candidate/wiodr13` passaram em modo `strict`, com identidade aprovada e zero artefatos ausentes/excedentes, diferenças arquiteturais ou exceções. A auditoria independente conferiu hashes de estado/job/outcome/comparação e dos resultados original/local: o original coincide com a ciência em D: e o local com a medição C2 correspondente. SHA-256 das comparações: baseline `f16cf0474a0064c78bdbae603a3075b61ee3944f6d679f3900df877393dbed31`; candidato `e59839bccff989227d71f44d0f66235f49086810bdbc30d1d35c3d816c309fe5`. As comparações WIOD16 ainda não foram executadas; nenhum certificado foi adotado por simples cópia ou mudança de path.

O recálculo completo WIOD13/estágio 5 passou nos limites de tempo e RSS: candidato de `09:37:37.8325625Z` a `09:39:49.1449783Z`, 131,3124158 segundos e 686.366.720 bytes; baseline de `09:40:20.6350317Z` a `09:42:27.4416960Z`, 126,8066643 segundos e 5.410.865.152 bytes. O candidato ficou abaixo tanto dos 152,16799716 segundos do critério relativo quanto dos 726,8066643 segundos do limite normativo com margem absoluta. Limite de RSS: 5.951.951.667 bytes. Os dois braços foram classificados como `controlled-serial-repeat`, sem sobreposição, exit zero, sem timeout e cluster encerrado sem processos remanescentes; job/outcome/resultado/métricas foram autenticados. SHA-256 de resultado/métricas: candidato `ec490c2e2065f245ad01a73023a340c40f3b76ac0830692dd93bb8f4190af935` / `389074e072b692004f5a6f2ee4519ac16f13f80d209e16b821944cce5df46e07`; baseline `b747002b53790fc47e11cc343b962d587516eedb37fb34569c3f123fc1e26a80` / `7cdecebd408897a6b4140dd618513d6582f90970f6827b67208ec030de4391f3`.

O par WIOD13/estágio 4 completo passou anteriormente: baseline de `09:24:19.5195653Z` a `09:32:13.0937094Z`, 473,5741441 segundos e RSS de 5.959.688.192 bytes; candidato de `09:32:44.7462088Z` a `09:37:05.3574485Z`, 260,6112397 segundos e RSS de 5.283.516.416 bytes. Limites aprovados: 1.073,5741441 segundos e 6.555.657.011 bytes. SHA-256 de resultado/métricas: baseline `ceb83d78f6b59f62d2374e0cb0208e5cf01d501e874f3a0e8b034c0c29f2d3f4` / `27a71c29770fd1d89fa6fc92ecb57cb812d09e78946d6fbb719d93a872ac563f`; candidato `ef2f3673b087ada2f94846bfae78010b09d31b9451a17f1957843d1d69c6bfea` / `a0bdeebed1e0f99a64374368a2d6b8b5a4b20b5adaa928a6f65670ac93b4e8d7`. A auditoria independente confirmou os hashes e identidades dos jobs/outcomes e as duas medições controladas, sem sobreposição ou processos remanescentes.

O braço baseline WIOD13/estágio 4/seletivo `gross-output-mv` também terminou: `09:42:58.7476678Z` a `09:45:08.7567100Z`, 130,0090422 segundos e RSS de 5.350.801.408 bytes; resultado `c76f4a72bd50c6c6dc49e5cb1ffb9a329ac480036a3f8f4bd71cd4825b67684a`, métricas `67d5d8cf1e1f25ac5bbc91c395d8670565ff9c33af90e758a40c972939d1e011`. Sua prova foi autenticada, com cluster encerrado e sem sobreposição. O candidato do par iniciou às `09:45:10.9139541Z`, PID `9152`; o gate desse par ainda depende de sua conclusão. Não houve alteração de código ou repetição de cenários durante a checagem das `09:45Z`.

O candidato WIOD13/estágio 4/seletivo `gross-output-mv` terminou de `09:45:41.1426092Z` a `09:48:00.8222074Z`, com 139,6795982 segundos e RSS de 1.937.993.728 bytes, concluindo o par com tempo/RSS aprovados. O limite relativo é 156,01085064 segundos; o normativo com margem absoluta, 730,0090422 segundos; limite de RSS, 5.887.672.320 bytes. SHA-256 de resultado/métricas: `b8f08a4016fcacda6f1beac21d0819864d5dc4043098d0500be3e14ec0658fbd` / `00b9a3ac0bd12d97baba9a75f41cad74e6c8e44a57a18d9f34cab69cde66fe26`. Hashes de job/outcome/resultado/métricas conferidos; medição controlada, sem sobreposição, timeout ou processos remanescentes.

O par WIOD13/estágio 5/seletivo `gross-output-du` passou: candidato de `09:48:33.3835296Z` a `09:50:33.1773659Z`, 119,7938363 segundos e RSS de 621.420.544 bytes; baseline de `09:51:04.8120031Z` a `09:52:53.2165413Z`, 108,4045382 segundos e RSS de 6.032.211.968 bytes. Limites: relativo de 130,08544584 segundos, normativo com margem absoluta de 708,4045382 segundos e RSS de 6.635.433.164 bytes. SHA-256 de resultado/métricas: candidato `f551485380419bfde5b9eb27bc80f84b539cd9cce0b1964f6c8f6ab1f7b556d3` / `b09858474265033bd80b3eb1917d09f32cb1a25ee2a6c404249676000707b3b0`; baseline `5b079a0d277417d5b963c46bf8fb0e5224ef5657cd61994b1578d76f65a9d262` / `31a3227c1d8c199bb3e21359e36d5593120c4848933b56645dbd1f6ed4a43810`. Auditoria independente confirmou hashes e identidade da seleção `gross_output.s.du`, medições controladas, cluster encerrado e ausência de sobreposição/processos remanescentes.

O report das `2026-09-05T09:52:55.2183364Z` registra sete de 14 pares aprovados, zero reprovados, 13 medições nativas e dois certificados de seeds restantes. A sequência WIOD13 com `workers=1` está completa; ainda falta o braço baseline WIOD13 `workers=2`, além dos seis pares WIOD16 `workers=1`. O worker baseline WIOD16/cálculo iniciou às `09:52:55.5074583Z`, PID `61436`, com identidade confirmada na consulta das `10:03Z`. O plano registra 49.642.201.308 bytes adicionais estimados para 81.303.134.208 bytes livres em C:, preservando o piso de 10 GiB. Preparação/falhas permanecem pendentes, sem sobreposição com o desempenho.

O cálculo completo WIOD16/`workers=1` passou no par controlado: baseline de `09:52:57.1767344Z` a `10:34:32.6131345Z`, 2.495,4364001 segundos e RSS de 20.097.941.504 bytes; candidato de `10:34:36.7163784Z` a `10:53:47.6844956Z`, 1.150,9681172 segundos e RSS de 15.542.808.576 bytes. Limites: relativo de 2.994,52368012 segundos, normativo com margem absoluta de 3.095,4364001 segundos e RSS de 22.107.735.654 bytes. Os braços são `controlled-serial-repeat`, sem sobreposição e com cluster encerrado. SHA-256 de resultado/métricas: baseline `7ab2d733f0854466b61c8cdb299ecfb784c6beff8c95ab446ea39da0ad9bf382` / `0b83118e55610adb2fa27711aba5e55131837b16fd6cb32590bcfe2f355f3148`; candidato `ef0dad72beeb9be50e7206560b626f90cae2a5d75a802d6d9f48ec61f1f80caf` / `853958a9ca195b5c6fa0129f74acb4df7c4dcd03a6e684587f7babeab6d239e5`.

O report das `2026-09-05T10:53:49.9115536Z` registra oito pares aprovados, zero reprovados e 11 medições nativas restantes. O mesmo controlador iniciou `seed-parity/baseline/wiodr16` às `10:53:50.2876313Z`, PID `25112`, após ambos os cálculos terminarem. Os dois certificados WIOD16 ainda são necessários antes dos recálculos locais. Não houve reinício do controlador nem alteração dos executáveis vinculados; preparação/falhas continuam aguardando o fim do desempenho.

Os dois certificados WIOD16 posteriormente passaram em modo `strict`, completando os quatro certificados D/C. A auditoria independente autenticou job, outcome, comparação e resultados original/local contra os estados e vínculos correspondentes. Ambos têm identidade aprovada, todos os artefatos aprovados e zero ausências, excedentes, diferenças arquiteturais, diferenças de indicadores ou exceções de política. SHA-256 das comparações: baseline `82ebf40e70bb6250dd4c26ad8315f2df090bc340250b38bb18de14d8cd0afeb1`; candidato `560327c7a8c86812ebe7cf7f952b5430fdf2a2b342ccd86546f0ba1b5eb2a569`.

A primeira tentativa de desempenho `candidate/recalculate/wiodr16/stage1/full` começou às `11:02:39.3744961Z`, worker PID `45832`; o R PID `53456` iniciou às `11:03:56.3031700Z`. A árvore de processos, incluindo o controlador PID `45640`, foi interrompida sem resultado final ou métricas completas. A última linha completa de telemetria é das `11:08:40.3569850Z`; a linha seguinte ficou truncada. O stderr do controlador está vazio e o do cenário contém somente a mensagem de início do recálculo. A causa não foi estabelecida: isso não demonstra divergência científica nem reprovação de tempo/RSS. O seed publicado no canal isolado da tentativa continuou sendo a única release visível nesse canal; o staging parcial foi preservado.

A recuperação explícita `run_logs/issue13-main-recover-performance-interruption-001.ps1` confirmou a ausência dos processos, preservou os arquivos e as 18 aprovações (14 medições nativas e quatro certificados), registrou somente a tentativa incompleta como `failed` e liberou `current`. O outcome de recuperação não é uma conclusão medida: seu fim `11:41:44.2153786Z` é apenas o limite superior conservador da constatação de ausência. As duas locks de controlador e a lock vazia de publicação foram movidas para destinos recuperáveis, sem exclusão. Pós-condições conferidas independentemente; estado científico e marker do seed permanecem idênticos. Registro em `binding-c2/control/performance/recoveries/interruption-001/recovery.json`, SHA-256 `bbc5805ab1602b19baeb52534d454e972a04758461d33aceb66912cf4a4b33da`; outcome inválido `9e6fa88a6304894fe430755c5f7648613ce5d47c25fd7c0205d3fb55eeacce04`; estado recuperado `3b5bd2a728435a66695b32f16d85a8b1a94159b5b7af2677e8ad5fbfface95dc`.

O mesmo `Plan` gerou `binding-c2/performance-plan-recovered-001.json`, SHA-256 `1307e76bf57ca455b4ccccbccba403ec1c31c5de8567f1cbeee1ce9d4232120f`: oito pares aprovados, zero reprovados, 11 medições nativas e zero comparações de seeds restantes. A estimativa adicional de 42.458.752.509 bytes cabe nos 71.571.726.336 bytes livres, preservando o piso de 10 GiB. A retomada usa os mesmos argumentos C2 com `-MaximumRepeats 11`, sem alteração de controlador, worker, biblioteca, motor, fontes ou vínculos. Registro `performance-c2-002.process.json`, SHA-256 `06dbda523cd9ba3b4d5e0765c1c030168fcd1b42171a9b0168af5a7612b79da4`: controlador PID `51444`, início `2026-09-05T11:42:35.8143826Z`. O worker PID `32672` iniciou `attempt-0002` às `11:43:10.8605272Z`. Nenhuma medição aprovada foi repetida; ciência, preparação e falhas não foram reiniciadas.

O par WIOD16/recálculo completo/estágio 1 passou em tempo e RSS. Candidato `attempt-0002`: `11:44:33.1731065Z` a `11:56:48.8555093Z`, 735,6824028 segundos e RSS de 15.941.386.240 bytes; baseline: `11:58:10.3030487Z` a `12:22:39.2851553Z`, 1.468,9821066 segundos e RSS de 18.797.146.112 bytes. Limites aprovados: relativo de 1.762,77852792 segundos, normativo com margem absoluta de 2.068,9821066 segundos e RSS de 20.676.860.723 bytes. Ambos são `controlled-serial-repeat`, sem sobreposição, exit zero, sem timeout e cluster encerrado sem processos remanescentes. SHA-256 de resultado/métricas: candidato `265502dad934b9fac7b8ba58b37ac89cb0673ba6138a2c205e3fcc975aba0868` / `7d2531b45bd879bf62cea966030a06a17acb202526e49fae8803f64faad32f8b`; baseline `da24f7364c0225cc63f20b5b182728af9f2a199b8afc930ab00dafde69eba57a` / `c9806355864183e3929ee3b04d89c07a888724de772d6431aa3cc822e7551318`.

O report das `2026-09-05T12:22:41.8402696Z` registra nove pares aprovados, zero reprovados e nove medições restantes. O worker baseline WIOD16/estágio 4 iniciou às `12:22:42.0890544Z`, PID `48636`, sem novo lançamento do controlador. A checagem das `12:35Z` confirmou o mesmo controlador PID `51444` e stderr vazio. Preparação e falhas continuam pendentes, sem executar R auxiliar durante as medições.

O par WIOD16/recálculo completo/estágio 4 passou em tempo e RSS: baseline de `12:24:00.8699465Z` a `12:47:39.9266237Z`, 1.419,0566772 segundos e RSS de 16.469.090.304 bytes; candidato de `12:49:02.1488399Z` a `13:00:06.1501963Z`, 664,0013564 segundos e RSS de 14.910.332.928 bytes. Limites aprovados: relativo de 1.702,86801264 segundos, normativo com margem absoluta de 2.019,0566772 segundos e RSS de 18.115.999.334 bytes. Ambos são `controlled-serial-repeat`, sem sobreposição. SHA-256 de resultado/métricas: baseline `d9c78a49bbbabfafee51f8a938a9d1bb116ab3556094326ddae184aaf32d49d3` / `af40348276b4cd1d73e84366cac9d17a053f40a75f3bdc357821f0521d002f4b`; candidato `771801655bad948055345b1610912bf79cef38c37358d3e06a35ea7405f39876` / `3752439d25dd817a17253fe7fdc715f016b64192478cd7f50ea5232b88531928`.

O candidato WIOD16/estágio 5 completo também terminou: `13:01:28.7978493Z` a `13:08:00.7065719Z`, 391,9087226 segundos e RSS de 2.766.102.528 bytes. Resultado `a2027b995e1f41429116d34db2abbdb533d6bc26de062adb2649f32b43285bc6`; métricas `aead658e638f12075fe45579b54ef439f385977e4ce12b00e79bf65c7d184f7c`. O par ainda aguarda o baseline, cujo worker PID `41896` iniciou às `13:08:03.9051372Z`. O report das `13:08:03.6661153Z` mantém dez pares aprovados, zero reprovados e seis medições restantes; o controlador não foi reiniciado nem alterado.

A auditoria independente das três novas medições (estágio 4 nos dois braços e candidato/estágio 5) confirmou hashes e identidade de jobs, outcomes, resultados, métricas e vínculos. Todas encerraram o cluster, com exit zero, sem timeout, sobreposição ou processos remanescentes. Nenhum cenário foi reexecutado para essa conferência.

O baseline WIOD16/estágio 5 completo terminou de `13:09:23.0382926Z` a `13:17:31.2300335Z`, com 488,1917409 segundos e RSS de 15.164.149.760 bytes. Resultado `870415516b062fef7d82a2e11c8e749c60875ad50bf017c7e7ea425f3f323d85`; métricas `9ee70590fc640c8e44c9baf58671ee2b56bd1b6ed15a5ba9b415432c56904f5a`. Isso conclui o 11º par: candidato de 391,9087226 segundos e RSS de 2.766.102.528 bytes, limites relativos/normativos de 585,83008908 / 1.088,1917409 segundos e RSS de 16.680.564.736 bytes. Ambos passaram em tempo/RSS com medição controlada e sem sobreposição.

O baseline WIOD16/estágio 4/seletivo `gross-output-mv` terminou de `13:18:52.4621649Z` a `13:26:41.6745976Z`, com 469,2124327 segundos e RSS de 19.264.147.456 bytes. Resultado `23c9d9285b7ba115699b4f8dfdf402f4fa273a96561bda86accd4f98aad068fe`; métricas `8c7a882e9f144a9d40046af2b9b98971a739c512c920386e7e989ceb1ffc7248`. O candidato correspondente iniciou às `13:26:45.0029932Z`, worker PID `64688`; sua telemetria está ativa, sem stderr do controlador. O report das `13:26:44.7292324Z` registra 11 pares aprovados, zero reprovados e quatro medições restantes. A checagem não iniciou R auxiliar, preparação ou falhas.

O smoke do journal executou dois processos PowerShell leves, um com saída zero e outro com erro sete, confirmando registro de ambos, fim real e preservação dos timestamps como strings UTC. Os testes conservaram medições anteriores/posteriores, recusaram a sobreposição efetiva e confirmaram que a atualização posterior do estado não amplia o intervalo histórico. Esse smoke não executou R e não constitui medição científica.

As repetições usam o mesmo worker e os mesmos commits, fontes e seeds autenticados da ciência, em canais e diretórios de tentativa próprios. Não repetem comparações já aprovadas e não alteram o estado da paridade. O roteiro utiliza os locks dos controladores da campanha, sem exigir exclusividade de todos os processos R do computador. Se os resultados adicionais não couberem com a folga prevista, ele bloqueia a execução para coordenação de novos roots vinculados; não remove evidência para liberar espaço.

Até os 14 pares terem medições controladas, o estado permanece `performance-pending`. Só depois aplica os limites normativos de tempo e RSS. Parser e smoke sintético cobriram UTC, singleton, fronteiras adjacentes, quatro tipos de sobreposição, valores exatos de aceitação/rejeição dos limites e matriz completa/incompleta. Intervalos das comparações antecipadas, inclusive a tentativa que falhou, também entram na classificação. Nenhuma repetição real de desempenho foi executada nesta implementação.

O plano serial agora soma armazenamento persistente cumulativo e o pico transitório de um único job, mantendo a reserva individual de 2,5 vezes com folga e o piso de 10 GiB. A ordem baseline/candidato alterna por fase. Medições científicas singleton válidas continuam reaproveitáveis; se um seed local adicional for necessário nesse caso, será contado explicitamente como cálculo auxiliar, nunca ocultado.

`tests/manual/issue13-main-provision-performance.ps1` provisionou quatro worktrees em `C:/Users/rodri/AppData/Local/wlvdb-issue13-performance-054`, nos mesmos commits e com 320 arquivos de fontes copiados e conferidos por tamanho/SHA-256 antes e depois: 9.964.040.970 bytes, cerca de 9,28 GiB. Não foram importados seeds nem executados cálculos. Configuração `campaign-performance-c1.json`, SHA-256 `caf142a87cc9c0f9c39be21dcde90967b19fc0f4fa508658b6e0a16c19a9d178`; inventário completo `provisioning.json`, SHA-256 `22e5b6a28f2cde414e4a0c8120544e0fa542264f6f2906ece5148f1e7a2c606c`. Nenhum arquivo original foi movido ou apagado. C: e D: são volumes do mesmo SSD físico; essa separação resolve capacidade, não isolamento de I/O.

A revisão independente identificou que um destino futuro aninhado em um root científico poderia contaminar a origem durante a cópia. O provisionador agora rejeita igualdade/ancestralidade com checkout, controle, evidência e os quatro roots científicos antes de qualquer escrita; também rejeita ancestrais com junction/symlink. Seis verificações de igualdade, descendência, ascendência, diferença de caixa e destinos disjuntos passaram sem escrita. O destino C: efetivamente utilizado já era disjunto; suas cópias e hashes não foram refeitos.

Os quatro cálculos controlados já necessários tornam-se seeds locais dos 20 recálculos, mantendo cada cadeia no mesmo `project_root`. Quatro comparações estritas D↔C precisam passar antes dos recálculos locais; o worker valida cada lado contra sua própria configuração e rejeita um seed D apresentado como nativo C. Não há importação com mudança de proveniência. O controlador usa locks da campanha e do suplemento, além do próprio root C; não exige exclusividade dos processos R do usuário.

O `Plan` de `2026-09-05T03:08:37Z` indica 28 medições nativas, quatro comparações de seeds e zero cálculos auxiliares; 60,28 GiB persistentes mais 5,12 GiB transitórios, total adicional de 65,40 GiB, cabem nos 88,96 GiB livres de C: preservando o piso. Evidências em `controller-checks/performance-c1-20260905T030837Z/`: `plan.json`, `synthetic-negatives.json` e `misbound-seed-negative.json`. Passaram parser, rejeição de runtime alterado, rejeição do seed com root incorreto, contagem explícita de seed auxiliar sintético, ordem alternada, intervalos abertos/fechados e contabilização serial de disco. Esses testes não executaram R. Reexecutar `Plan` com os tamanhos finais antes de `Run`; aguardar o encerramento dos 28 cenários e de todos os intervalos/locks da campanha.

```powershell
$performanceCampaign = 'C:\Users\rodri\AppData\Local\Packages\OpenAI.Codex_2p2nqsd0c76g0\LocalCache\Local\wlvdb-issue13-performance-054\binding-c2\campaign-performance-c2.json'
& $runner -NoProfile -File tests/manual/issue13-main-performance.ps1 -Action Plan -ConfigPath $campaign -ExecutionConfigPath $performanceCampaign -ComparisonBindingPath $comparisonBinding
# Somente depois dos cenários científicos e dos comparadores encerrarem:
& $runner -NoProfile -File tests/manual/issue13-main-performance.ps1 -Action Run -ConfigPath $campaign -ExecutionConfigPath $performanceCampaign -ComparisonBindingPath $comparisonBinding -MaximumRepeats 25
```

## Critérios ainda necessários para encerrar o gate

A paridade de cálculo/recálculo e a equivalência `workers=1/2` estão aprovadas no snapshot científico acima, incluindo dimensões e dimnames, máscaras de `NA`/`NaN`, estados semânticos, metadados, matrizes, diagnósticos e células não selecionadas. Não há nova tolerância científica. Preservar os vínculos de fontes/artefatos e acrescentar os resultados dos gates restantes ao relatório final.

Executar as medições controladas de tempo e memória. O limite temporal é `max(1,2 × baseline, baseline + 600 segundos)`; o de RSS é `baseline + max(10% do baseline, 512 MiB)`. Tempos coletados sob concorrência científica são observacionais e não fecham esse critério. A revisão operacional 054v2 mantém orçamento de 80 GiB e reservas por job de 8/22/40 GiB (WIOD13, WIOD16 e `workers=2`), com piso ajustado de 24 para 16 GiB livres; o roteiro de desempenho lê os valores vinculados na configuração, sem impor novos limites.

Anexar a comparação das preparações, os dez resultados de falha com rollback e ausência de release parcial, o encerramento dos clusters, os registros da CI Windows/Ubuntu já aprovada e o resultado agregado final. Até essa evidência estar completa, manter o PR sem aprovação de merge e o #13 aberto.
