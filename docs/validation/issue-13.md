# Issue 13 — validação do corte principal

Estado em 2026-09-05: **relatório parcial; gate de merge pendente**. Este registro não constitui aprovação integral de paridade científica, desempenho ou publicação. Os registros anteriores em `run_logs/` e nas campanhas externas permanecem preservados; seus resultados não são convertidos automaticamente em evidência da campanha atual.

## Escopo e referências

- Métodos executáveis: `wiodr13` e `wiodr16`. Dez métodos experimentais permanecem declarados, com cálculo e recálculo desabilitados; seus contratos e testes unitários isolados foram preservados.
- Referência científica original: merge do #12, `cc2c861`. Baseline executável de compatibilidade: `e2f4d6dae9a6d35c966b305fabac52e489faa3e7`, discriminado do commit original.
- Candidato congelado para a campanha 054: `972d9f8fc7a887b3db485080264f2958cce13cdd`.
- Configuração local efetiva: `D:/Trabalho/Code/wlvdb-issue13-main-054/campaign-v2.json`; configuração anterior preservada em `campaign.json`.
- Tooling científico: cópia privada do runtime externo da campanha 053, vinculada por arquivo e hash. Comparação: derivada separada em `comparison-tooling-v1/`, sem alterar o tooling ou o estado científico; não representa adoção de resultados científicos anteriores.
- Matriz: 14 pares científicos (cálculo, recálculos 1/4/5, seleções 4/5 e `workers=2`), duas preparações e dez falhas. São 40 cenários nessa matriz, além dos seeds auxiliares, e 41 comparações previstas.
- Papers não integram este corte.

As preparações usam os mesmos seis caches oficiais: WIOD13 (`WIOTS_in_MATLAB.zip`, `Socio_Economic_Accounts_July14.xlsx`), WIOD16 (`WIOTS_in_R.zip`, `Socio_Economic_Accounts.xlsx`) e EU KLEMS (`Statistical_Capital.rds`, `Statistical_National-Accounts.rds`). A verificação normativa de tamanhos e SHA-256 será executada antes da preparação; a igualdade real ainda não foi atestada por esta campanha.

## Evidência registrada

| Verificação | Resultado conhecido |
| --- | --- |
| Catálogo, configuração, perfis de saída e agregação | Suítes focadas passaram durante o fechamento do corte. |
| Execução nativa, planejador e consistência dos slices terminais | Suítes focadas passaram com fixtures; experimentais habilitados apenas em catálogos de teste em memória. |
| Catálogo publicado em `docs/methods.md` | Regeneração e `--check` passaram. |
| Suíte completa de fixtures | 66 arquivos passaram em quatro shards antes do commit `972d9f8`; registros `unit-tests/shard1.json` a `shard4.json`, com 242,865 / 387,4972 / 97,33 / 249,79 segundos. |
| CI | Execuções de `972d9f8`, `ed3fdc0`, `a73d344`, `175e287`, `8543089`, `1783add`, `12064f3`, `8dcb7a1`, `bb7af19` e `a5881c9` concluídas com sucesso. A última é `33947718413`; a anterior, `33945755195`, terminou no Ubuntu às `05:03:54Z` e no Windows às `05:10:12Z`. |
| Controlador suplementar | Parser passou; smoke de processo verificou captura de logs, erro de processo e escrita atômica de estado. |
| Ambiente dos filhos | Teste confirmou 35 variáveis removidas e dez definidas; biblioteca R preservada. |
| Isolamento e retomada do suplemento | Casos de igualdade/ancestralidade de paths e identidade PID + horário de início passaram. |
| Inicialização suplementar | Plano de 12 registros construído e auditor independente passou. Vínculo do controlador atualizado após sua estabilização, preservando a inicialização provisória. Nenhum cenário de preparação/falha foi executado. |
| Execução científica real | Os 28 cenários passaram; a campanha terminou às `2026-09-05T04:39:53.906680Z`, estado `science-completed`, revisão 103. Todos os registros de processos confirmam cluster encerrado. Isso ainda não constitui paridade integral. |
| Comparação antecipada WIOD13 | Segunda tentativa passou às `2026-09-05T02:31:28Z`: 21 artefatos, zero diferenças de indicadores e nenhum artefato ausente/excedente. Oito diferenças arquiteturais foram validadas pelas regras explícitas existentes. Sem repetição de cálculo. |
| Comparação antecipada WIOD16 | Passou às `2026-09-05T02:59:38Z`: 21 artefatos, zero diferenças de indicadores e nenhum artefato ausente/excedente. Oito diferenças arquiteturais foram validadas pelas mesmas regras explícitas. Sem repetição de cálculo. |
| Infraestrutura de desempenho | Quatro roots autenticados em C: provisionados, sem iniciar R de desempenho. Plano atual: 25 medições nativas restantes, três medições reutilizáveis, quatro comparações estritas de seeds e zero cálculos extras de seed. |
| Comparações finais | Consulta às `2026-09-05T06:26:43Z`: 22 de 38 execuções concluídas, duas ativas e 14 planejadas. Nove paridades aprovadas: os seis cenários WIOD13 `workers=1` e, no WIOD16, cálculo completo e recálculos completos 1/4. Treze verificações internas do oráculo foram coletadas; sua avaliação conjunta ainda está pendente. |
| Paridade real, desempenho, preparação e dez falhas | Gate pendente; os 28 cenários científicos terminaram, mas as comparações e demais provas ainda não estão completas. |

A inicialização suplementar provisória terminou em `2026-09-05T01:01:48Z` (2026-09-04 no horário local). Seu plano tem SHA-256 `7fa43b85892dc2b34ccd9113c21d0edfa31a7d0241bb127037101210093732c3`; o auditor, `f08bdc7cafb39136fb1de0de6a77fddffe54052fed0f9376f970f80f7a55a950`.

Após estabilização do controlador compartilhado, a vinculação foi atualizada em `2026-09-05T01:05:37Z`, reaproveitando o mesmo plano/auditor, sem repetir o builder nem executar cenários. Os arquivos anteriores de estado e vínculo foram preservados, com hashes conferidos, em `D:/Trabalho/Code/wlvdb-issue13-main-054/control/supplemental/initializations/before-controller-finalization/`. O novo `tooling-binding.json` tem SHA-256 `9547fc55f2f322b1db655053fac3969b71ed2accc4e82f06ffbd6ee942c7cc16`. O estado confirmado é `initialized`, com zero cenários e zero comparações concluídos no suplemento.

## Ações operacionais

As duas primeiras tentativas de setup da 054 não produziram evidência científica: a primeira falhou na captura de `LASTEXITCODE` após inspeção Git; a segunda detectou que o PowerShell vinculado havia sido atualizado pelo aplicativo antes de iniciar R. Os diagnósticos foram preservados. A revisão operacional `054v2` mantém os mesmos roots de dados e separa controle/evidência em `control-v2` e `evidence-v2`; foi vinculada a uma cópia privada do PowerShell para não depender de futuras atualizações do aplicativo. Esses eventos não são resultados de paridade nem novos RunAll científicos.

Na revisão v2, a tentativa 1 parou no bootstrap em cerca de quatro segundos, antes do cálculo: o layout do checkpoint não correspondia ao contrato histórico do harness. A correção ficou restrita ao frontend das novas tentativas: `specs` e `evidence` passaram a ser diretórios irmãos sob a mesma base, mantendo `job` e `outcome` no controle. A relação exigida entre o diretório de especificações e o terceiro ancestral da evidência do cenário foi conferida. Não houve alteração científica, recriação da campanha ou substituição dos roots; a tentativa anterior e seus diagnósticos foram preservados.

A tentativa 2 começou às `2026-09-05T01:23:32Z` (22h23min32 BRT de 2026-09-04), com controlador PID `58912`. O estado das `01:23:34Z` registra os quatro cálculos simultâneos como `running`: WIOD13 e WIOD16, baseline e candidato, todos com `workers=1`. Os logs baseline já alcançaram transformação de dados e Leontief, distinguindo esta execução real dos erros anteriores de bootstrap. O resultado científico ainda precisa terminar e ser comparado.

O canal público permanece intacto: a campanha opera nos seus canais e roots isolados, sem promover os resultados em andamento. A automação existente de acompanhamento foi reativada com o prompt atualizado para o escopo dos dois métodos principais; ela não amplia o escopo do gate nem constitui autorização de merge ou publicação.

O controlador científico PID `58912` encerrou após os 28 cenários, sem lock remanescente. O resumo compacto `evidence-v2/science-execution-summary.json` reúne os quatro vínculos de fontes e, por cenário, caminhos/hashes das provas, commits, intervalos, tempo, RSS e encerramento dos processos; SHA-256 `18c4fc78636ce0f44b1ab699db7f3f9c110ad0690f5a26020881e8bb0f0d8391`. Sua classificação é explicitamente execução, não certificação de paridade. A transição para as comparações usou os mesmos roots/commits, sem recalcular. Comando, PID, início e hash do controlador de comparação estão em `comparison-v2-001.process.json`; logs em `comparison-v2-001.stdout.log` e `.stderr.log`.

O controlador de comparação PID `46776`, iniciado às `2026-09-05T04:44:20.8588714Z`, permanece ativo com identidade conferida. As seis paridades WIOD13 aprovadas cobrem cálculo `workers=1`, recálculos completos 1/4/5 e seletivos 4/5. Das dez verificações desse método contra os respectivos parent runs, duas tiveram igualdade estrita e oito registraram diferenças permitidas para avaliação posterior. Nesses oito casos, `status=passed` atesta execução válida, não aprovação das diferenças: a validação normativa dos dez deltas do oráculo continua em `0/10`. Não há certificação global nem equivalência `workers=2` concluída nesta consulta. O scheduler trabalha em lotes de até dois comparadores e aguarda o encerramento do lote antes de abrir o seguinte; seu código não foi alterado durante a execução.

A comparação final `parity/calculate/wiodr16/workers1` terminou às `2026-09-05T05:24:30.8382430Z`, com código zero e `comparison.passed=true`: 21/21 artefatos aprovados, quatro arrays sem divergências, nenhuma diferença de indicadores e nenhum artefato ausente/excedente inesperado. O contrato validou as oito diferenças arquiteturais declaradas e o recurso exclusivo do candidato. Prova: `evidence-v2/comparison-attempts/parity__calculate__wiodr16__workers1/attempt-0001/comparison.json`, SHA-256 `3e3d0a8c0985d1a281c006549a4006993d57fb97cc96049c2daa2606f32d546e`, conferido contra estado e outcome. O wrapper PID `64524` encerrou; na consulta das `05:35:41Z`, o recálculo WIOD16/estágio 1 do mesmo lote ainda estava ativo. Nenhum cenário científico foi repetido para obter essa comparação.

A paridade do recálculo completo WIOD16/estágio 1 terminou às `2026-09-05T05:41:17.0727432Z`, também com 21/21 artefatos aprovados e nenhuma divergência de arrays ou indicadores. Prova em `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage1__full/attempt-0001/comparison.json`, SHA-256 `671527c2d02657be2a59e716c6cf4b5392a0bd14e5cec7eda7044e6028194ad7`. Suas comparações contra os parent runs registraram somente `_anomalies.csv` no baseline e `_runtime_resources.rds` no candidato, respectivamente; a aceitação normativa dessas diferenças permanece pendente. Os hashes dos três reports foram conferidos contra o estado e os outcomes.

A paridade do recálculo completo WIOD16/estágio 4 terminou às `2026-09-05T06:22:29.7613497Z`, com código zero, `comparison.passed=true`, 21/21 artefatos aprovados e nenhuma diferença de indicadores ou artefato ausente/excedente inesperado. Prova em `evidence-v2/comparison-attempts/parity__recalculate__wiodr16__stage4__full/attempt-0001/comparison.json`, SHA-256 `05ea982898d767aac1d68554a8d0517336b9cac72126945950ca553df5160d37`. O oráculo baseline do mesmo cenário terminou às `05:54:35.6889859Z`, com igualdade nos quatro arrays e diferença somente em `_anomalies.csv`; SHA-256 `64eedb488493d9bd4da9c3cc4dd1ad1fc0dbeed917b6586660af4e277c7bd73d`. Os hashes conferem com estado e outcomes. Esse resultado do oráculo não é aprovação normativa de seu delta; a avaliação conjunta permanece pendente. O lote seguinte iniciou às `06:22:30Z`, com o oráculo candidato do estágio 4 e a paridade completa do estágio 5.

A comparação antecipada de WIOD13 (`early/parity/wiodr13/001`), executada de `01:44:49Z` a `01:47:23Z`, preservou comando, entradas e hashes em `early-parity-wiodr13-001/`. O erro `Candidate runtime generation differs from metadata derivation.` veio do perfil V5 derivado em `3ae99a848156a28431ff44cf4d9e619c6de84a83`, que espera geração `4668bf1eadb9ea3ea3f121a889f6b29016c1505f1dbcb3cc27ed3c02ab52c2dd`; o candidato congelado apresenta `600d8cdd2c692fea0b608285b84c3d260b123aac4cf1904ee8b6b997ec988c63`. O diagnóstico integral está em `execution-error.json`. Não houve `comparison.json`, escrita no estado científico ou reinício dos cálculos. O próximo ajuste deve reconstruir e verificar a equivalência dos metadados a partir dos commits congelados, em um vínculo de comparação separado; não basta substituir o hash esperado nem modificar o harness utilizado pelos processos científicos ativos.

Esse ajuste foi implementado por `tests/manual/issue13-main-build-metadata.R`: definições e configurações foram reconstruídas em três worktrees limpos, somente de código, nos commits `cc2c861`, `e2f4d6d` e `972d9f8`. Nenhum payload científico foi aberto ou calculado. Os seis sidecars do baseline original e do oráculo executável são exatamente iguais. As 12 tabelas por braço dos dois métodos também são iguais aos perfis históricos, sem diferenças de esquema, linhas, células ou ordem. As únicas três diferenças de envelope são commit candidato, geração e redução de 12 para dois métodos. Os negativos de método, geração, célula e ordem passaram.

Artefatos locais em `comparison-tooling-v1/`:

| Artefato | SHA-256 |
| --- | --- |
| `metadata-derived.json` | `8476d0389d7cdaf87c8d63e2be569ad00d4978627869810f560bc26fceecaf23` |
| `metadata-derivation-provenance.json` | `dde0c2e6b1c1c4dfaefa42341349c7bc67149f238ae1687fe30616fa6642dbdb` |
| `metadata-diff-vs-v5.json` | `edee1e23c59d43113ede4389677b38278a143435cd85bd7fc9a6bf369b74b161` |
| `comparison-binding.json` | `b464b4c29255feed2a30a41c59f22d9ef5df4dd9304efc01dcfd62e37871e3d9` |

O vínculo separado conserva 45 dos 47 arquivos byte a byte; muda somente o perfil de metadados e, deterministicamente, os guards de commit e métodos do validador. Também autentica os dois braços de execução, a proveniência semanticamente validada, snapshots do controlador e contratos de entrada por lado. Paridade aceita os commits diferentes dos dois motores; comparações internas usam o mesmo braço. Retomada com outro vínculo é rejeitada. Os 25 testes positivos/negativos passaram em `comparison-selftest-001/selftest.json`, sem alteração no tooling científico. A segunda comparação antecipada usa o worker `/2` e registra evidência em `early-parity-wiodr13-002/`; é diagnóstico antecipado, não adoção automática entre as 38 comparações finais.

A segunda comparação antecipada WIOD13 terminou com `passed=true` e `comparison_passed=true`, de `02:28:07Z` a `02:31:28Z` (cerca de 201 segundos). SHA-256 de `comparison/comparison.json`: `bf820579c98f74cc7803998dc2030445fdc7dab69e169a1186372118b233fcca`. O wrapper PID `50612` e o R PID `45888` encerraram. A comparação antecipada WIOD16, com o mesmo vínculo, em `early-parity-wiodr16-001/`, também passou: de `02:34:00Z` a `02:59:38Z`, cerca de 1.538 segundos; SHA-256 de `comparison/comparison.json`: `e6df7054cc0226149c38320616f73c887dea58144ac9dce3782d3b625c0273b4`. Seu wrapper PID `35104` e R PID `50552` encerraram. Ambas leem os cálculos já autenticados, não escrevem no estado principal e não repetem ciência. São provas antecipadas, ainda não adotadas automaticamente entre as 38 comparações finais. O commit de tooling `8543089` contém a correção; o candidato científico permanece em `972d9f8`.

### Reúso exato de provas de arrays

Os oito pares FST dos cálculos completos têm payloads byte-idênticos entre os motores. `tests/manual/issue13-main-array-proof-*` permite reaproveitar somente o retorno de `wlv13_compare_fst_array` já produzido nas duas comparações antecipadas autenticadas. A chave inclui par ordenado de motores/método, hashes e tamanhos dos payloads, papel lógico, sidecars semânticos completos com atributos, dimensões/dimnames, esquema FST, modo, chunking e hashes dos algoritmos/vínculos. Qualquer incompatibilidade segue pela comparação integral; nenhuma tolerância é acrescentada. Inventário, proveniência, validações antes/depois, demais artefatos e veredito continuam no comparador original.

O cache determinístico `array-proof-cache-v1/array-proof-cache.json`, 238.432 bytes, tem SHA-256 `2490e32895d34ea9029e2a24182bcbc609fb70d345329830508d35a6294d2326`, explicitamente permitido tanto pelo loader R quanto pelo validador PowerShell. Isso impede aceitar um cache fabricado apenas por ele declarar hashes e origens válidas. A reconstrução em `verification/cache.rebuilt.json` foi byte-idêntica. O selftest reproduziu exatamente os oito retornos reais e passou em 14 negativos, incluindo payload, nomes/atributos dos eixos, esquema, proof, contexto/motores, modo e chunking; os modos `strict` e `cross_engine_source_v1` com contexto nulo produzem miss. Log final: `array-proof-cache-v1/verification/selftest-final-14.stdout.log`, SHA-256 `2b2cd8356ea5f974d8fd512a5bdd4e0fff46db1df375b22ddad9ec264d8c0a61`. Não houve leitura dos payloads nesse selftest.

O canário de ponta a ponta `array-proof-canary-wiodr13-001/` passou com quatro hits e descompressão pelo comparador original proibida no teste. Os três CSVs foram byte-idênticos aos da comparação antecipada; o JSON foi semanticamente idêntico, excetuando somente `scenario_id` e `compared_at`. SHA-256 do novo `comparison/comparison.json`: `60682f894f464deed8d4c2c2cea309251cf239ec3121c48cb97fd7c507bdfbf9`. Seu intervalo conservador, de `03:34:50Z` até a confirmação de encerramento do PID em `03:39:24.8919588Z`, está em `comparison-interval.json` (SHA-256 `b414208ff1979c34ec08081f8afde4f1ac891a07feaf5580197aae09049e1cf9`) e é incluído no planejamento de desempenho. É diagnóstico sob carga concorrente, não medição normativa nem execução de cálculo.

O vínculo suplementar final é `array-proof-binding-v2/array-proof-binding.json`, SHA-256 `66d6fc7f75cfab3f946058fe9d0c6e49ff649d507791531e5afcdee867cd7504`. Ele possui snapshots próprios de entrypoint, biblioteca, builder, validador e cache; os 47 arquivos do vínculo original `b464...` permanecem inalterados. O snapshot provisório v1 foi preservado, não utilizado no controlador principal, após uma correção de miss para modos incompatíveis. A retomada verifica todos os attempts históricos antes do reparo, inclusive os já aprovados, e não permite habilitar, desabilitar ou trocar o vínculo silenciosamente. Passaram 45 verificações PowerShell em `array-proof-binding-selftest-004/selftest.json`, sem R/cálculo. A comparação integral dos quatro seeds de desempenho D↔C permanece no modo `strict`, sem este cache.

A inspeção dos demais perfis alcançáveis não exigiu mudanças: oito SHA-256 de `_unit_contract.csv`/`_source_manifest.csv` dos dados normalizados dos dois métodos/braços coincidiram com o perfil de preparação histórico. Os quatro `_anomalies.csv` de cálculo coincidiram com os fingerprints históricos dos bridges e parents de estágio 5; o target baseline WIOD13/estágio 1 também coincidiu. Os outros cinco targets de recálculo ainda precisam ser conferidos quando concluídos. Os perfis históricos de preparação, bridges e multiplicidade permaneceram byte-idênticos; seus artefatos e contratos atuais continuam sujeitos à comparação efetiva.

O suplemento da revisão v2 foi inicializado e auditado às `2026-09-05T01:18:52Z`, sem executar os cenários de preparação/falha: zero cenários e zero comparações concluídos. SHA-256 do plano: `e4ea4f7c1d575657d30699a9f721502ceeed7c34cb86ff5b76018322bf31dc72`; vínculo do tooling suplementar: `4cd8afad9dfc8b716713583ea500b8f126464c526c33561a9c59138041ac1fa2`. O controle anterior permanece preservado.

Às `2026-09-05T01:44:29Z`, somente o controlador suplementar foi reautenticado para registrar intervalos individuais de processos. Estado e vínculo anteriores foram preservados, com igualdade dos hashes confirmada, em `control-v2/supplemental/initializations/before-process-journal-20260905T014428967Z/`. O novo vínculo tem SHA-256 `73988e10a28537ce6f3e6fcec8ce6e1bb0f949c0f027f8746322b681563045fd`. Plano, auditor e inventário de logs permaneceram iguais: nenhum builder, auditor R, preparação ou cenário foi repetido. O journal começa vazio e conserva o limite histórico anterior em `01:18:52Z`, sem estendê-lo até a reautenticação. O estado científico não foi alterado.

Às `2026-09-05T03:03:19Z`, a validação anterior à importação do seed das falhas foi reforçada: resultado e métricas devem coincidir com os hashes registrados pelo estado científico, além de método, root, commit e encerramento do cluster. O seed real passou; alterações independentes dos dois hashes foram rejeitadas antes de R/importação. Somente o vínculo do controlador suplementar foi atualizado, para SHA-256 `602cfd1638a8a02359f0150b8e20af18f157aadd3d2240db9514190254e632ae`; a versão anterior está em `control-v2/supplemental/initializations/before-seed-provenance-20260905T030319669Z/`. Estado, plano e journal permaneceram byte-idênticos, com zero preparações/falhas executadas.

Executar a partir do checkout, usando o PowerShell selado definido na configuração. `Status` é somente leitura. As ações científicas e suplementares devem ser coordenadas pelo mesmo orçamento de recursos; não iniciar `Prepare` ou `Faults` como carga adicional não contabilizada enquanto o pool científico estiver cheio.

```powershell
$campaign = 'D:\Trabalho\Code\wlvdb-issue13-main-054\campaign-v2.json'
$runner = 'D:\Trabalho\Code\wlvdb-issue13-main-054\tools\powershell\pwsh.exe'
$comparisonBinding = 'D:\Trabalho\Code\wlvdb-issue13-main-054\comparison-tooling-v1\comparison-binding.json'
$arrayProofBinding = 'D:\Trabalho\Code\wlvdb-issue13-main-054\array-proof-binding-v2\array-proof-binding.json'
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Plan
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Initialize -ConfigPath $campaign
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action RunScience -ConfigPath $campaign -Arm all -MaxJobs 4
& $runner -NoProfile -File tests/manual/issue13-main-compare.ps1 -ConfigPath $campaign -ComparisonBindingPath $comparisonBinding -ArrayProofBindingPath $arrayProofBinding -MaxJobs 2
& $runner -NoProfile -File tests/manual/issue13-main-gate.ps1 -Action Status -ConfigPath $campaign
```

O suplemento usa `tests/manual/issue13-main-supplemental.ps1 -ConfigPath $campaign -Action <ação>`:

| Ação | Efeito |
| --- | --- |
| `Initialize` | Constrói e audita o plano; não prepara dados nem calcula resultados. |
| `Prepare` | Confere os seis caches e executa as preparações baseline/candidata. |
| `Compare` | Compara semanticamente as preparações e os três seletores de artefatos. |
| `Faults` | Exige preparação comparada e cálculo candidato WIOD13 aprovado; importa o seed, prepara dez canais, executa dez falhas e valida o agregado transacional. |
| `RunAll` | Executa a sequência suplementar completa. |
| `Status` | Lê `control/supplemental/state.json`. |

### Desempenho controlado, separado da ciência

`tests/manual/issue13-main-performance.ps1 -ConfigPath $campaign -Action Plan` é somente leitura. Ele classifica os 14 pares, identifica quais braços podem ser aproveitados e estima o espaço adicional antes de qualquer repetição. `Status` recalcula a mesma classificação; `Run -MaximumRepeats N` executa no máximo N cálculos/recálculos necessários, sempre em série, após os 28 cenários científicos passarem. As comparações estritas dos seeds não consomem esse contador. `-ExecutionConfigPath` separa os roots/estado de desempenho dos roots científicos; `-ComparisonBindingPath` mantém o comparador autenticado da campanha original.

Uma medição científica só recebe `controlled-reused-singleton` quando seu intervalo UTC não se sobrepõe a nenhum outro job, comparador, preparação ou falha da campanha, incluindo tentativas malsucedidas e seeds auxiliares. O suplemento registra cada processo com início e fim reais em UTC, PID, ação e resultado, inclusive falhas; medições anteriores ou posteriores não são invalidadas retroativamente. Intervalos interrompidos sem encerramento comprovado ficam abertos. Somente o histórico anterior ao journal usa exclusão conservadora, com limite final fixado na migração; executar ações novas não estende esse limite sobre medições já concluídas.

WIOD13 baseline `workers=2` passou de `03:35:31.5580961Z` a `03:45:06.5480211Z`: 574,989925 segundos, pico RSS de 7.417.114.624 bytes e cluster encerrado. Essa medição **não** é reutilizável para desempenho, pois o canário de arrays se sobrepôs até `03:39:24.8919588Z`. WIOD16 baseline e os dois candidatos `workers=2` poderão fornecer até três medições reutilizáveis, somente após conclusão válida e confirmação de ausência de sobreposição. A admissão observada está limitando `workers=2` a um cenário de cada vez: duas reservas de 40 GiB mais piso de 16 GiB exigem 96 GiB livres; havia cerca de 68,17 GiB livres na consulta. O controlador congelado e as reservas não foram alterados para acelerar essa parte.

O `Plan` consultado às `04:23Z` confirmou duas medições `controlled-reused-singleton`: WIOD16 baseline, de `03:45:08.7291869Z` a `04:12:50.7079642Z`, 1.661,9787773 segundos e RSS de 21.301.354.496 bytes; WIOD13 candidato, de `04:12:52.8579963Z` a `04:19:59.9454732Z`, 427,0874769 segundos e RSS de 5.445.861.376 bytes. Ambos registraram dois workers, cluster encerrado e nenhuma sobra de processos. Restam 26 medições nativas e quatro comparações estritas dos seeds nesse plano parcial, ainda sem pares completos de desempenho. O candidato WIOD16 permanece pendente; WIOD13 baseline continua explicitamente excluído do reúso pela sobreposição com o canário.

Após a ciência terminar, o candidato WIOD16 `workers=2` também foi classificado como `controlled-reused-singleton`: `04:20:02.0302796Z` a `04:39:53.6647718Z`, 1.191,6344922 segundos e RSS de 16.656.515.072 bytes, dois workers, cluster encerrado e nenhum processo remanescente. SHA-256 do resultado: `e6139c9a0893772d30eeef3bab270255aa7f2c00fb4b2f7a06944a34a86a822d`; métricas: `a157a186a294f05a2ed82509443f1551af5c05274de336603b458b3bf2e58308`. O par WIOD16 `workers=2` passou: limite de tempo de 2.261,9787773 segundos e limite de RSS de 23.431.489.945 bytes. Há um de 14 pares completos, três medições reutilizáveis, 25 medições nativas restantes, quatro comparações estritas de seeds e zero seeds extras.

O snapshot `evidence-v2/performance-plan-science-completed.json`, SHA-256 `feb2046a2ce8e11e260ec0d01716c3e6d220883886f98ae3fe9dae4fecdabbba`, registra essa classificação. A estimativa adicional caiu para cerca de 57,77 GiB e cabe em C: preservando o piso de 10 GiB. Ainda não foram executadas repetições controladas no root C. Uma inspeção somente leitura também confirmou que a preparação usa o mesmo limite temporal com margem absoluta de 600 segundos e o mesmo critério de RSS; nenhum ajuste de fórmula ou de harness foi necessário.

O smoke do journal executou dois processos PowerShell leves, um com saída zero e outro com erro sete, confirmando registro de ambos, fim real e preservação dos timestamps como strings UTC. Os testes conservaram medições anteriores/posteriores, recusaram a sobreposição efetiva e confirmaram que a atualização posterior do estado não amplia o intervalo histórico. Esse smoke não executou R e não constitui medição científica.

As repetições usam o mesmo worker e os mesmos commits, fontes e seeds autenticados da ciência, em canais e diretórios de tentativa próprios. Não repetem comparações já aprovadas e não alteram o estado da paridade. O roteiro utiliza os locks dos controladores da campanha, sem exigir exclusividade de todos os processos R do computador. Se os resultados adicionais não couberem com a folga prevista, ele bloqueia a execução para coordenação de novos roots vinculados; não remove evidência para liberar espaço.

Até os 14 pares terem medições controladas, o estado permanece `performance-pending`. Só depois aplica os limites normativos de tempo e RSS. Parser e smoke sintético cobriram UTC, singleton, fronteiras adjacentes, quatro tipos de sobreposição, valores exatos de aceitação/rejeição dos limites e matriz completa/incompleta. Intervalos das comparações antecipadas, inclusive a tentativa que falhou, também entram na classificação. Nenhuma repetição real de desempenho foi executada nesta implementação.

O plano serial agora soma armazenamento persistente cumulativo e o pico transitório de um único job, mantendo a reserva individual de 2,5 vezes com folga e o piso de 10 GiB. A ordem baseline/candidato alterna por fase. Medições científicas singleton válidas continuam reaproveitáveis; se um seed local adicional for necessário nesse caso, será contado explicitamente como cálculo auxiliar, nunca ocultado.

`tests/manual/issue13-main-provision-performance.ps1` provisionou quatro worktrees em `C:/Users/rodri/AppData/Local/wlvdb-issue13-performance-054`, nos mesmos commits e com 320 arquivos de fontes copiados e conferidos por tamanho/SHA-256 antes e depois: 9.964.040.970 bytes, cerca de 9,28 GiB. Não foram importados seeds nem executados cálculos. Configuração `campaign-performance-c1.json`, SHA-256 `caf142a87cc9c0f9c39be21dcde90967b19fc0f4fa508658b6e0a16c19a9d178`; inventário completo `provisioning.json`, SHA-256 `22e5b6a28f2cde414e4a0c8120544e0fa542264f6f2906ece5148f1e7a2c606c`. Nenhum arquivo original foi movido ou apagado. C: e D: são volumes do mesmo SSD físico; essa separação resolve capacidade, não isolamento de I/O.

A revisão independente identificou que um destino futuro aninhado em um root científico poderia contaminar a origem durante a cópia. O provisionador agora rejeita igualdade/ancestralidade com checkout, controle, evidência e os quatro roots científicos antes de qualquer escrita; também rejeita ancestrais com junction/symlink. Seis verificações de igualdade, descendência, ascendência, diferença de caixa e destinos disjuntos passaram sem escrita. O destino C: efetivamente utilizado já era disjunto; suas cópias e hashes não foram refeitos.

Os quatro cálculos controlados já necessários tornam-se seeds locais dos 20 recálculos, mantendo cada cadeia no mesmo `project_root`. Quatro comparações estritas D↔C precisam passar antes dos recálculos locais; o worker valida cada lado contra sua própria configuração e rejeita um seed D apresentado como nativo C. Não há importação com mudança de proveniência. O controlador usa locks da campanha e do suplemento, além do próprio root C; não exige exclusividade dos processos R do usuário.

O `Plan` de `2026-09-05T03:08:37Z` indica 28 medições nativas, quatro comparações de seeds e zero cálculos auxiliares; 60,28 GiB persistentes mais 5,12 GiB transitórios, total adicional de 65,40 GiB, cabem nos 88,96 GiB livres de C: preservando o piso. Evidências em `controller-checks/performance-c1-20260905T030837Z/`: `plan.json`, `synthetic-negatives.json` e `misbound-seed-negative.json`. Passaram parser, rejeição de runtime alterado, rejeição do seed com root incorreto, contagem explícita de seed auxiliar sintético, ordem alternada, intervalos abertos/fechados e contabilização serial de disco. Esses testes não executaram R. Reexecutar `Plan` com os tamanhos finais antes de `Run`; aguardar o encerramento dos 28 cenários e de todos os intervalos/locks da campanha.

```powershell
$performanceCampaign = 'C:\Users\rodri\AppData\Local\wlvdb-issue13-performance-054\campaign-performance-c1.json'
& $runner -NoProfile -File tests/manual/issue13-main-performance.ps1 -Action Plan -ConfigPath $campaign -ExecutionConfigPath $performanceCampaign -ComparisonBindingPath $comparisonBinding
# Somente depois dos cenários científicos e dos comparadores encerrarem:
& $runner -NoProfile -File tests/manual/issue13-main-performance.ps1 -Action Run -ConfigPath $campaign -ExecutionConfigPath $performanceCampaign -ComparisonBindingPath $comparisonBinding -MaximumRepeats 28
```

## Critérios ainda necessários para encerrar o gate

Consolidar os resultados reais por cenário, IDs e hashes das fontes/artefatos, dimensões e dimnames, máscaras de `NA`/`NaN`, estados semânticos, metadados, matrizes, diagnósticos, células não selecionadas e equivalência `workers=1/2`. Registrar todas as diferenças e aplicar apenas as tolerâncias científicas já normativas.

Executar as medições controladas de tempo e memória. O limite temporal é `max(1,2 × baseline, baseline + 600 segundos)`; o de RSS é `baseline + max(10% do baseline, 512 MiB)`. Tempos coletados sob concorrência científica são observacionais e não fecham esse critério. A revisão operacional 054v2 mantém orçamento de 80 GiB e reservas por job de 8/22/40 GiB (WIOD13, WIOD16 e `workers=2`), com piso ajustado de 24 para 16 GiB livres; o roteiro de desempenho lê os valores vinculados na configuração, sem impor novos limites.

Anexar a comparação das preparações, os dez resultados de falha com rollback e ausência de release parcial, o encerramento dos clusters, os registros da CI Windows/Ubuntu já aprovada e o resultado agregado final. Até essa evidência estar completa, manter o PR sem aprovação de merge e o #13 aberto.
